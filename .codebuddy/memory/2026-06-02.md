# 2026-06-02 工作日志

## 双语国际化改造
- 为 Kilwa 矿区管理系统添加了中英文双语支持
- 创建 `static/js/i18n.js`：包含约 150+ 条中英翻译字典，`t()` 翻译函数，`applyI18n()` DOM 遍历引擎，`switchLang()` 切换函数（含自动重渲染活跃页面）
- 修改 `templates/index.html`：
  - 加载 i18n.js
  - 导航栏最右侧（month-selector 前）添加 `中文 | EN` 语言切换按钮组
  - 所有静态 HTML 元素添加 `data-i18n` / `data-i18n-placeholder` / `data-i18n-title` 属性
  - 所有 JS 动态生成文本替换为 `t('key')` 调用（Toast、Dashboard 标签、TYPE_LABELS、薪资统计、核对面板、图表标题、日工资表头、出勤标签、审计日志、模态框表单等）
  - `TYPE_LABELS` 改为 `getTYPE_LABELS()` 函数，语言切换时动态更新
- 修改 `static/css/style.css`：添加 `.lang-toggle-group` / `.lang-toggle-btn` 样式，暗色科技风主题，当前语言高亮
- 语言偏好存入 `localStorage`（key: `kilwa_lang`），刷新后保持
- 语言切换时自动调用当前活跃页面的渲染函数重新渲染

## Cloud Studio 部署
- 部署到 http://c7c4c1ff23f74c2ca67394c73586262e.codebuddy.cloudstudio.run
- 云端数据验证：130 名员工，应发合计 50,600,002 TZS（与本地一致）
- 注意：Cloud Studio 部署时需用 `files` 参数显式指定 .xlsx 源文件，否则默认不会上传

## Git 推送
- Commit: `4e1c6d7` - feat: 双语国际化支持 + 数据更新
- 11 files changed, 995 insertions, 408 deletions
- 已 push 到 origin/main

## 修复重复计薪 Bug
- 修改 `core/calculator.py`：
  - `calc_underground_piece`：白班/夜班 `valid` 列表按 eid 去重（同一人同天多次出现只计一次）
  - `calc_day_salary`：来源1(出勤表)和来源2(产量表)增加 `(eid, date) in counted_pairs` 去重，且 A/L 检查优先于 counted_pairs 标记（避免 A/L 误占槽位）
  - `compute_daily_breakdown`：相同逻辑修复
- 对比分析结果：
  - 井下计件：0 人受影响（当前数据无重复）
  - 日薪：5 人被重复计算（NASRA YAHAYA、MJUNI、ACRAM、BAHATI、DAVID DANIELY），共多算 82,000 TZS
- 钻工跨队长不修复（设计如此）
- 待 git commit & push
