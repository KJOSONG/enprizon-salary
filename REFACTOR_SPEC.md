# 薪资系统重构 — 需求与架构说明书

> **状态**：草案 v1（需求结构化基线，待逐节评审）
> **最后更新**：2026-08-12
> **目标读者**：项目 Owner（KEJU）、实施 Agent
> **来源**：多轮需求梳理会话（矿区管理评分系统 + 现有 enprizon-salary 看板系统）

---

## 1. 背景与目标

### 1.1 现状痛点
当前薪资数据在 **3 个地方产生、本系统只"算"不"收"**：

```
企业微信表单(入职/离职/请假) ─┐
纸质记录(入职/调岗/离职)       ─┼─→ 人工整理成 Excel ─→ 本系统上传计算
现场表单(出勤/产量)            ─┘
```

- 生命周期事件（入职/调岗/离职/薪资变）散落在企微 + 纸质，本系统要靠**人工再录一遍**才生效 → 漏录即漏薪（调岗问题即源于此）。
- 评分系统是另一套独立 Excel（井下出渣工人），与本系统 5 轨计薪分家。
- 现有本系统偏"看板"：多个页面是不同维度的仪表盘，平铺 tab、无父子页、月份切换不丝滑。

### 1.2 重构目标
将本系统打造为 **唯一的"采集 + 计算 + 展示"网页平台**，吸收企微与纸质的采集职能，让生命周期事件**自动驱动**薪资，消灭重复录入。

### 1.3 非目标（本期不做）
- 工人自助端（工人完全离线、现金发薪，无账号）。
- 对接企业微信 API（企微全弃用）。
- 斯瓦希里语（仅中英双语）。
- 可视化拖拽表单生成器（采用配置驱动）。

---

## 2. 关键约束（不可妥协的前提）

| # | 约束 | 影响 |
|---|------|------|
| C1 | **现金发薪，工人完全离线，无工人账号** | 全管理端；工人侧输出走打印/导出（工资条、评分公示卡） |
| C2 | **企微全弃用**，网页为唯一采集入口 | Excel 上传降级为"批量导入/过渡"通道，非主路径 |
| C3 | **仅中英双语**（无斯语） | i18n 维持中/英，不增斯语 |
| C4 | **旧数据只读归档**，不回溯、不事件化 | 新系统全新起；旧 `kilwa.db` 冻结为只读镜像 |
| C5 | 井下评分系统 = 当前**井下计件工人**（同一批人） | 其 `salary_type` 可在"计件"↔"评分模型"间切换 |
| C6 | 司机津贴需**单源化** | 避免与现有 `_apply_driver_allowance` 双重发放 |

---

## 3. 范围与扩展性

- **本期范围**：以井下工人（含其评分模型）为首个完整落地场景。
- **扩展性预留**：架构按"可加部门"设计（员工/计薪类型/表单均参数化），将来可纳入其他部门，不预写其他部门逻辑。

---

## 4. 信息架构（导航树）

```
工作台(总览)
├─ 员工管理
│   ├─ 员工列表（搜索/筛选：部门·班组·在职状态）
│   └─ 员工档案（点姓名进入）
│       ├─ 基本信息（录入/编辑）
│       ├─ 生命周期时间线（入职/调岗/离职/薪资变/技能变，可筛选查询）
│       ├─ 请假记录（可查询）
│       └─ 关联数据入口（当月薪资/出勤/产量/评分）
├─ OA 审批
│   ├─ 入职 │ 调岗 │ 离职 │ 薪资/技能变更（提交→审批）
├─ 考勤
│   ├─ 出勤网格（可手动改 P/A/L，含请假类型区分）
│   └─ 请假审批（仅需审批的类型）
├─ 产量采集
│   └─ 井下/钻工/破碎 录入（班组长，仅本组，手机优先）
├─ 评分（井下）
│   ├─ 评分卡录入 │ 汇总 │ 三闸面板
├─ 薪资
│   └─ 薪资总表 │ 日工资 │ 核对 │ 导出
└─ 系统
    ├─ 用户与权限 │ 表单自定义 │ 计薪参数 │ 旧数据归档
```

- 顶部常驻：**周期上下文条**（当前月 + 顺滑上/下月切换 + 懒加载），替代现有别扭选择器。
- `员工管理` 与 `OA 审批` 分离：前者是"已存在员工档案"，后者是"发起变更的流程"，二者通过事件联动。

---

## 5. 业务域与能力详述

### 5.1 员工档案（聚合根）
员工是系统的中心实体。档案页包含：
- **基本信息**（可录入/编辑）：姓名、部门、岗位、技能等级、入职日、**NIDA 证件号**、电话、**NSSF 社保号**。
- **生命周期时间线**：入职/调岗/离职/薪资变/技能变事件，按时间排序，可筛选查询。
- **请假记录**：见 5.3。
- **关联数据入口**：跳转当月薪资、出勤、产量、评分。

入职 OA 表单字段 ≈ 员工基本信息；审批通过后自动生成员工档案，之后可在档案页继续编辑。

### 5.2 OA 审批流（事件驱动）
| 流程 | 提交人 | 审批人 | 效果 |
|------|--------|--------|------|
| 入职 | 管理/班组长 | 你(KEJU) | 生成员工档案（基本信息落库） |
| 调岗 | 管理 | 你 | 改部门/轨道/岗位，带生效日 |
| 离职 | 管理 | 你 | 置离职+结束日，停止计薪，入离职表 |
| 薪资/技能变更 | 管理 | 你 | 改月薪/日薪/技能等级，带生效日 |

所有流程产出**带 `effective_date` 的事件**，系统按月推导员工当月状态，自动驱动计薪，无需二次录入。`overrides` 逐步被事件流取代。

### 5.3 请假管理
- **请假类型**：
  - **事假**：需审批（班组长代提 → 你批）。
  - **年假 / 调休**：当前每人 **每月 4 天**自由调休额度，**无需审批**；自动累积，显示**年假余额**（余额 = 累积 − 已休）。
- **查询**：支持**按员工查** + **全局按时间/类型搜**（如"查所有 8 月年假"）。
- **出勤体现**：请假须在出勤表中**单独体现**（事假、年假区分显示，区别于 A 旷工 / 普通 L），不混入普通出勤状态。

### 5.4 考勤
- 月度网格保留，支持手动 P/A/L 纠错（当前机制保留，无问题）。
- 请假（5.3）审批/记录后自动落对应出勤状态，并在网格中以类型区分呈现。

### 5.5 产量采集
- Web 表单直写 DB（复用现有 parser 写入的同一批表）：井下/钻工/破碎分轨道日度录入。
- 班组长仅可录**本组**数据，手机优先。

### 5.6 评分模型（井下，可选模块）
- 6 维匿名互评卡 Web 录入（无评分人列，保匿名）→ 去极值 + 管理 1.5 票 → 行为分 → 系数。
- 客观层 R1/R2 → S → 发放比例；三层奖金模型。
- 月收入 = 技能阶梯固定薪资 + 驾驶员津贴(单源) + 个人奖金，并入净额。
- 防串通三闸面板前端展示。

### 5.7 薪资计算与展示
- 5 轨（井下计件/钻工/破碎/日薪/月薪）+ 评分模型统一；双路径核对保留。
- 净额公式并入出渣工人的固定薪 + 司机津贴(单源) + 评分奖金。
- 薪资总表 / 日工资 / 核对 / 导出（7 Sheet）纳入出渣工人行。

### 5.8 系统管理
- 用户与权限（见第 6 节）。
- 表单自定义（见第 7 节）。
- 计薪参数（单价/NSSF 费率/奖金池等，沿用 `settings` + `/config`）。
- 旧数据归档（只读，见第 10 节）。

---

## 6. 角色与权限模型

- **预设角色**：`super_admin`(KEJU) > `admin`(中方管理) > `manager`(本地主管) > `team_lead`(班组长) > `viewer`(只读)。
- **细粒度**：权限 = **(模块, 动作, 数据范围)** 三元组；用户除继承角色外，可**单独增删**某项权限（满足"不同用户不同权限"）。
- **数据范围**：全员 / 指定班组（班组长仅本组）。
- 典型映射：班组长 = 产量+出勤+评分录入（本组）、请假代提；管理 = 全矿录入+查看；你 = 全部+审批+配置。

---

## 7. 表单引擎（配置驱动，无拖拽）

- **不做**可视化拖拽布局生成器（工作量/维护成本极高）。
- **采用 schema 驱动**：每张表单在 DB 以"字段定义"描述（字段名/类型/标签/选项/校验/必填/可见角色）。前端统一渲染器按 schema 出表单；管理端提供"选类型+填标签+设选项/必填/显隐"的简单界面维护字段。
- 标准字段落表列，自定义字段存 JSON 附列。
- 入职表单字段即员工基本信息字段，亦走此机制；日后加"紧急联系人"等，后台点几下即可，不必改代码。

---

## 8. 概念数据模型（待实现阶段细化）

| 表 | 用途 |
|----|------|
| `employees` | 员工主档（基本信息：姓名/部门/岗位/技能等级/入职日/NIDA/NSSF/电话） |
| `employee_events` | OA 生命周期事件（类型/effective_date/快照/操作人） |
| `leave_requests` | 请假（类型/提单人/审批人/状态/余额联动） |
| `attendance` | 出勤（含请假类型区分） |
| `production` | 产量（分轨道日度） |
| `score_cards` | 评分卡（6 维，无评分人列） |
| `form_schemas` / `form_fields` | 表单自定义 |
| `permissions` / `user_grants` | 细粒度权限 |
| `archived_kilwa` | 旧库只读镜像 |

> 现有 11 张表（`overrides`/`attendance_overrides`/`bonus_penalties`/`dismissed_employees` 等）沿用并演进；`attendance_overrides` 继续承接手动/请假覆盖。

---

## 9. 非功能需求

- **移动优先**：班组长录入页手机优先、大按钮、中英切换。
- **i18n**：中/英双语（无斯语）。
- **UX**：月份切换顺滑（上下文条 + 懒加载）；页面引入父子层级与过渡。
- **输出**：工资条、评分公示卡支持打印/导出（现金无账号）。
- **演进式**：在现有 Flask + SQLite + SPA 底座上扩展，不推倒重写。

---

## 10. 迁移策略

- 新系统启用后，**旧 `kilwa.db` 冻结为只读归档**（`archived_kilwa`），不事件化、不回溯。
- Excel 上传保留为批量导入通道（过渡期纸质→Excel→导入仍可用）。
- 新数据全从网页录入。

---

## 11. 实施路线（分阶段，每阶段可独立上线）

- **P0** 需求定稿（本说明书评审通过）+ 角色/权限模型 + 表单 schema 设计 + 导航重构方案
- **P1** 员工档案 + OA 生命周期事件化（入职/调岗/离职/薪资变自动驱动计薪）→ 直击最大痛点
- **P2** 考勤网格重构 + 请假（类型/余额/查询/出勤区分）+ 产量 Web 录入（班组长本组）
- **P3** 评分模型并入（入库 + Web 录入 + 并入净额 + 三闸面板）
- **P4** 角色细化 + 表单自定义后台 + 导航/月份切换 UX 打磨
- **P5** 旧数据归档接入 + Excel 导入通道收尾

---

## 12. 开放问题（待定）

1. 基本信息字段：先落最小集（5.1 所列）还是一次性更全？
2. 事假审批流细节：班组长代提→你批，是否需要驳回/转交？
3. 年假额度规则：固定 4 天/月，是否区分试用期/离职结算？
4. 评分模型上线优先级：是否延后到 P3（先解生命周期+采集痛点）？
5. 导航重构（P4）是否提前与 P1 同做，还是独立后做？
