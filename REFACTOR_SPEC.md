# 薪资系统重构 — 需求与架构说明书 v6

> **状态**：v6（PRD 更新，2026-08-12；在 v5 验收基础上对齐 10 条有效反馈）
> **最后更新**：2026-08-12
> **目标读者**：项目 Owner（KEJU）、实施 Agent、新接手者
> **来源**：v5 PRD（2026-08-12）+ v5 验收后 10 条有效反馈（筛选 bug/OA 自审规则/请假子页/通知/审批人设定/中英双语）
> **依赖**：`AGENTS.md`、`DEV_WORKFLOW.md`、`ARCHITECTURE.md`（v3 历史快照）、`docs/P0_DATA_MODEL_AND_API.md`、`docs/P12_OA_PROFILE_COLLECTION_REFINEMENT.md`、`docs/P13_OA_NOTIFY_QA_REFINEMENT.md`

## 0. 变更摘要（v3 → v4）

| 域 | v3 状态 | v4 变化 |
|----|---------|---------|
| 顶部交互 | 登录前月份选择器、scope 搜索、员工列表+档案双页 | 取消登录前月份选择、scope 搜索→单一全局、员工档案页删除并入列表 |
| 员工档案 | 8 列（姓名/部门/岗位/入职/NIDA/NSSF/电话/银行） | 增 性别 / 出生日期 / 年龄（计算）/ 头像 / 班组 / 自定义工号 / 字段类型严格化 / 薪资类别+基数在档案内可改 |
| OA 模块 | 顶级模块 | 删除顶级，3 流程嵌入员工管理底部 |
| 请假 | 3 类（事假/年假/调休） | + 病假（免审、14 天/年默认余额） |
| 请假申请 | 输入员工 ID | 改"姓名/工号"模糊搜；员工列表显示 ID |
| 产量采集 | nav_production，含 D/N 切换 | 改名 `数据采集` (nav_collection)；井下/钻工单表内含 Day+Night 双段；+ 出勤收集表 |
| 钻工组表单 | 单队长单条 | N 队长槽位（可加减），参考表单样式 |
| 破碎表单 | 复用 attendance | 独立表单：Date + Bags + 出勤 |
| 评分模块 | 6 维匿名卡，班组硬编码 1/2 | 班组命名 LAMBA LAMBA / SAKA SAKA，月份由顶层决定，选班组自动出全员按工号排序，一张张卡录入 |
| 系统 | 含源文件管理、源数据上传 | 删除源文件管理 UI（API 保留为批量导入过渡） |
| 司机津贴 | 旧 `_apply_driver_allowance()` 按部门名/岗位匹配 | 删旧逻辑；保留 driver_roster + 出勤勾选；薪资总表新增 `driver_allowance` 列 |
| 实施路线 | P0-P5 | + P6-P11 |

## 0.1 变更摘要（v4 → v5，验收后 10 条有效反馈）

| # | 域 | v4 状态 | v5 变化 |
|---|----|---------|---------|
| 1 | OA 三申请层级 | 入职/调岗/离职 3 按钮嵌在员工列表页底部卡片，内联小表单 | **独立为员工管理侧栏 3 个子页**（员工列表下方平级）；入职申请 = 完整员工档案表单（全部基本信息字段），审批通过后**自动列入员工列表** |
| 2 | 员工档案形态 | 行点击 → modal 弹窗抽屉；头像 56px 圆形 | **独立完整档案页**（非弹窗，hash `#employees/profile?id=`）；头像改为**简历照尺寸**（建议 96×120px，3:4，方形圆角） |
| 3 | 假期余额 | 弹窗只读显示年假/调休/病假余额，仅病假可调 | 档案页显示**三类余额且均可修改**（年假/调休/病假），修改写审计 |
| 5a | 姓名选中行为 | 列表行/姓名链接 onclick 即导航，无法选中复制 | 鼠标选中姓名仅选中（可复制，`user-select:text`），**点击才跳转**档案页 |
| 4 | 井下出渣采集 | 一行只标 `NICKEL(H)`，三个输入框无列头；Remark 仅一个且夹在 Day/Night 之间 | **三列带标签** NICKEL(H) / NICKEL(L) / MAWE；**Day 与 Night 各自独立 Remark** |
| 5b | 钻工组采集 | 队长下拉列全部钻工部门员工；按钮"添加队长"直接加空槽 | 队长下拉**只列队长名单内人员**（当前 3 人）；按钮改名"**添加队伍**"，流程 = 先选队长 → 再生成队伍列，逻辑与默认 3 队一致 |
| 6 | 评分模块命名 | 侧栏模块 ID `nav_scoring` 缺 i18n 键 → 直接显示原始键名 | 改名"**评分系统**"，补齐中/英 i18n 键 |
| 7 | 驾驶勾选位置 | 驾驶勾选在**出勤收集**每人一行 | 移到**井下出渣采集**：Day/Night 选择人员弹层内每人勾选（仅司机名单内可勾）；出勤收集**去掉驾驶**，改为**按部门**选择提交（井下/钻工/破碎以外部门） |
| 8 | 选中框样式 | 各表单选人后的白色显示框样式内联不统一，错位 | 统一为公共 `emp-chips` 样式类，全部采集表单对齐 |
| 9 | 计薪参数页 | 计算参数配置仍显示"司机津贴"（全局手工金额） | **删除该卡片**与 saveConfig/resetConfig 中的 `driver_allowance` 字段 |
| 10 | 计薪参数页 | 原话"计薪参数页面加入…"（未写完） | **用户确认为笔误，忽略本条**（不新增任何参数项） |

> 注：用户反馈编号沿用原话（含重复的 #5），上文 1/2/3/5a/4/5b/6/7/8/9/10 对应原 11 点；其中 #10 已确认为笔误，实际有效反馈 10 条。

---

## 0.2 变更摘要（v5 → v6，验收后 10 条有效反馈）

| # | 域 | v5 状态 | v6 变化 |
|---|----|---------|---------|
| 1 | 员工列表部门筛选 | 下拉可选但**筛选不生效** | **修复**：`renderEmployeeTable()` 重建下拉时保留当前选中值（根因：每次渲染重置 select 选中项） |
| 2 | 侧栏员工子页 | 员工档案/入职/调岗/离职均列在侧栏 | **员工档案从侧栏移除**（档案应由列表点员工进入，不设独立侧栏子页；保留 hash 直达 `#employees/profile?id=`） |
| 3 | OA 自审规则 | 批准禁止自审（所有人），驳回不限制 | **分角色**：super_admin 可批准自己提交；其他角色自己提交只能驳回不能批准（驳回保持放开）。审批卡片**可展开查看表单详情**（尤其入职 18 字段） |
| 4 | 请假申请入口 | "新建请假"按钮嵌在待审批页 | **移出待审批页**，改为侧栏"员工列表"下方平级子页"请假申请" |
| 5 | 侧栏折叠底部区 | 折叠后中英切换/登录/改密/退出样式粗糙 | 优化折叠态布局（图标化/紧凑对齐） |
| 6 | 入职申请头像 | 入职表单无头像字段 | 入职表单**加入头像上传**（PNG/JPG ≤2MB，同档案页） |
| 7 | 入职提交后 | 提交成功留在原页 | **提交后自动进入待审批页**（OA 待审 Tab） |
| 8 | 审批通知 | 无通知机制 | 新增**站内通知铃铛/红点**：顶部通知图标，红点提示，点开列出"待你审批"事项 |
| 9 | 审批人设定 | 审批人固定（提交人不可选） | **超级管理员后台设定指定审批人**（不是提交人操作）；未指定时走默认审批流 |
| 10 | 中英双语 | 113 处硬编码中文未 i18n 化 | **全页面字段中英双语审查**：页面展示字段补齐 i18n 键，中英切换完整无遗漏 |

> 注：10 条均归入 **P13** 阶段实施（见 §11-P13 与 `docs/P13_OA_NOTIFY_QA_REFINEMENT.md`）。

---

## 1. 背景与目标

### 1.1 现状痛点
当前薪资数据在 **3 个地方产生、本系统只"算"不"收"**：

```
企业微信表单(入职/离职/请假) ─┐
纸质记录(入职/调岗/离职)       ─┼─→ 人工整理成 Excel ─→ 本系统上传计算
现场表单(出勤/产量)            ─┘
```

- 生命周期事件（入职/调岗/离职/薪资变）散落在企微 + 纸质，本系统要靠**人工再录一遍**才生效 → 漏录即漏薪。
- 评分系统是另一套独立 Excel（井下出渣工人），与本系统 5 轨计薪分家。
- 现有本系统偏"看板"：多个页面是不同维度的仪表盘，平铺 tab、无父子页、月份切换不丝滑。
- 顶部交互冗余：登录前月份选择器与顶部 period-bar 重复；搜索 scope 下拉不导航。
- 员工档案与列表字段重叠，切换页面无实质收益。
- 产量采集页面设计不贴合现场人工录入习惯，缺少 D+N 同表单、缺多队长槽位、缺历史可再编辑。
- 评分录入只支持逐条添加，无班组全员一览、无自定义工号、无一周一卡直观录入。

### 1.2 重构目标
将本系统打造为 **唯一的"采集 + 计算 + 展示"网页平台**，吸收企微与纸质的采集职能，让生命周期事件**自动驱动**薪资，消灭重复录入。

**v4 增量目标**：
- 顶部交互清理（去冗余、修 bug、统一）
- 员工体验统一（合并列表+抽屉）
- 员工档案完整性（性别/年龄/头像/字段类型）
- OA 合并入员工板块
- 数据采集模块完整重设计（贴合参考表单）
- 评分模块重设计（班组+全员+自定义工号）
- 系统清理（删源文件管理、删司机津贴独立设置、薪资总表新列）

**v5 增量目标**（v4 验收后 10 条有效反馈）：
- OA 三申请（入职/调岗/离职）升格为员工管理**平级子页**，入职申请承载**完整档案表单**，审批后自动入列表
- 员工档案从弹窗改**独立完整页面**，头像简历尺寸
- 档案页显示并支持修改三类假期余额
- 员工姓名可选中复制、点击才跳转
- 井下出渣采集补齐 NICKEL(H)/(L)/MAWE 三列标签 + Day/Night 双备注 + 驾驶勾选
- 钻工队长名单化（当前 3 人，计薪参数页维护），"添加队伍"先选队长再生成队伍列
- 出勤收集按部门提交、移除驾驶勾选
- 采集表单选人框统一样式对齐
- 计薪参数页删除旧司机津贴项（全局手工金额）；司机津贴单价沿用既有默认 5,000/班
- 评分模块改名"评分系统"并补 i18n

**v6 增量目标**（v5 验收后 10 条有效反馈）：
- 修复员工列表部门筛选不生效
- 员工档案子页从侧栏移除（档案仍可从列表进入）
- OA 自审规则分角色（super_admin 可自批；其他角色可自驳不可自批）；审批卡片可展开查看表单详情
- 请假申请升格为侧栏平级子页，移出待审批页
- 优化侧栏折叠后底部区样式
- 入职表单加入头像上传
- 入职提交后自动进入待审批页
- 新增站内通知铃铛/红点（列出"待你审批"事项）
- 审批人由超级管理员后台指定
- 全页面字段中英双语审查（消灭硬编码中文）

### 1.3 非目标（本期不做）
- 工人自助端（工人完全离线、现金发薪，无账号）。
- 对接企业微信 API（企微全弃用）。
- 斯瓦希里语（仅中英双语）。
- 可视化拖拽表单生成器（采用配置驱动）。

---

## 2. 关键约束（不可妥协的前提）

| # | 约束 | 影响 |
|---|------|------|
| C1 | **现金发薪，工人完全离线，无工人账号** | 全管理端；工人侧输出走打印/导出（工资条、评分公示卡） |
| C2 | **企微全弃用**，网页为唯一采集入口 | Excel 上传降级为"批量导入/过渡"通道，非主路径 |
| C3 | **仅中英双语**（无斯语） | i18n 维持中/英，不增斯语 |
| C4 | **旧数据只读归档**，不回溯、不事件化 | 新系统全新起；旧 `kilwa.db` 冻结为只读镜像 |
| C5 | 井下评分系统 = 当前**井下计件工人**（同一批人） | 其 `salary_type` 可在"计件"↔"评分模型"间切换 |
| C6 | **司机津贴仅井下出渣工人产生**，由**井下出渣采集**选人弹层内勾选"驾驶"触发，且须在校验**司机名单**内方可勾选；按班计津贴自动流入薪资 | 车辆管控（防乱用）+ 津贴精准计发；不再依赖旧通用 `_apply_driver_allowance` |
| C7 | **字段类型严格化** | 日期/电话/枚举字段在前端受 schema 约束，禁止存文本字符串到日期字段；schema 错误立即报错，不静默 |
| C8 | **数据采集 = 单一网页录入路径** | Excel 上传仅作批量导入过渡，UI 隐藏入口；班组长手机优先 |
| C9 | **数据采集与计薪自动联动** | 出勤 A/L 员工从当日计件分配排除，剩余人员平分（v3 守恒） |

---

## 3. 范围与扩展性

- **本期范围**：以井下工人（含其评分模型）为首个完整落地场景，覆盖井下/钻工/破碎/其他部门员工。
- **扩展性预留**：架构按"可加部门"设计（员工/计薪类型/表单均参数化），将来可纳入其他部门，不预写其他部门逻辑。

---

## 4. 信息架构（导航树）

```
工作台(数据台)
├─ 员工管理
│   ├─ 员工列表（点击行 → 跳转独立档案页，非弹窗）
│   ├─ 入职申请（完整档案表单，审批后自动入列表）
│   ├─ 调岗申请
│   └─ 离职申请
├─ 考勤
│   ├─ 出勤网格（手动 P/A/L，自动落 S/Y/T/P(病)）
│   ├─ 出勤收集表（花名册点选模式，按部门选择）
│   └─ 请假申请/审批（事假/病假/年假/调休）
├─ 数据采集（取代旧 nav_production）
│   ├─ 井下出渣采集（D+N 双段同表，三列标签 + 双备注 + 驾驶勾选）
│   ├─ 钻工组采集（N 队伍槽位，队长名单化）
│   ├─ 破碎计件采集（bags + 出勤）
│   └─ 出勤收集表
├─ 评分系统（改名自旧 nav_scoring）
│   ├─ 评分卡录入（班组+全员+一张张卡）
│   ├─ 评分汇总
│   └─ 三闸面板
├─ 薪资
│   ├─ 薪资总表（+ driver_allowance 列；井下计件列→生产薪资）
│   ├─ 日工资明细
│   ├─ 核对
│   └─ 导出
└─ 系统
    ├─ 用户与权限
    ├─ 表单自定义
    └─ 计薪参数
```

**顶部常驻**：
- **周期上下文条（period-bar）**：当前月 + 上下月切换 + 懒加载
- **全局搜索（无 scope）**：单一入口，防抖 300ms，跨 employees/salary/production/attendance/leave/scorecard

**删除**：
- 顶级 `nav_oa` 模块（流程并入员工管理）
- 登录前 `#monthPickerModal`
- 搜索 scope 下拉
- 系统→源文件管理 UI（API 保留）
- 员工列表底部"人事申请"按钮卡片（v5 升级为独立子页）
- 员工档案弹窗（`employeeProfileModal`，v5 升级为独立页面）
- 司机津贴独立设置入口（统一由出勤勾选 + 司机名单管控）

---

## 5. 业务域与能力详述

### 5.1 员工档案（v5：独立完整页面）

员工是系统的中心实体。**v4 曾把档案并入列表抽屉（弹窗）**；v5 依反馈**恢复为独立完整页面**——点击员工列表行/姓名进入 `#employees/profile?id=<employee_id>`，是完整的子页面而非弹窗。

#### 5.1.1 列表字段（必显）
| 字段 | 备注 |
|------|------|
| 员工 ID | **新增列**：通讯录账号（如 `111`、`128`、`005`） |
| 姓名 | **可选中复制**（`user-select:text`），点击才跳转档案页 |
| 性别 | **新增** |
| 年龄 | **新增**：由出生日期计算（不存） |
| 部门 | |
| 岗位 | |
| 班组 | **新增** |
| 入职日期 | |
| NIDA | |
| NSSF | |
| 薪资类别 | |
| 薪资基数 | 日薪/月薪 |
| 状态 | active/dismissed/suspended |
| 操作 | 查看档案（点击） |

> **v5 交互**：鼠标在姓名上选中只做选中/复制，不触发跳转；点击姓名或行 → 跳转独立档案页。

#### 5.1.2 档案页内容（独立子页）
- **基本信息卡**：姓名、电话（+255 默认）、性别、出生日期、部门、岗位、班组、技能等级、入职日、NIDA、NSSF、银行名称/账号/户名、头像（上传/预览/删除）
- **头像**：**简历照尺寸**，建议 96×120px（3:4，方形圆角），区别于原 56px 圆形小图；上传预览与列表小头像共用同一文件
- **生命周期时间线**：入职/调岗/离职/薪资变/技能变事件，按时间排序，可筛选查询
- **请假记录**：按时间列出，含状态/驳回原因
- **假期余额卡（v5 可修改）**：年假 / 调休 / 病假三类余额（已用/总额），每类带"调整"按钮，改 `leave_balances` 的 `total_entitled` / `used`，写审计日志
- **关联数据入口**：跳转当月薪资、出勤、产量、评分

#### 5.1.3 字段类型与编辑约束
| 字段 | 类型 | 约束 |
|------|------|------|
| 姓名 | text | 必填 |
| 性别 | select | 男/女/其他 |
| 出生日期 | date | YYYY-MM-DD datepicker；年龄 = floor((今天 - 出生日) / 365.25) 计算 |
| 电话 | phone | 默认前缀 `+255`；仅数字可填入；不强制有 |
| 部门 | select | 枚举（不可自由文本） |
| 岗位 | text | 自由文本（区别于部门/薪资类别） |
| 班组 | select | 枚举（关联评分班组） |
| 技能等级 | select | 枚举（Junior/Senior/Lead 等） |
| 入职日 | date | YYYY-MM-DD |
| NIDA | text | 非空时校验格式 |
| NSSF | text | 关联 nssf_enrolled=1 |
| 银行名称/账号/户名 | text | 可选 |
| 薪资类别 | select | piece_underground / piece_driller / piece_crush / day_rate / monthly |
| 薪资基数（日薪/月薪） | number | 与薪资类别联动 |
| 头像 | file | image/png/jpeg；≤2MB；存 `static/avatars/<employee_id>.<ext>` |

**薪资类别+基数在档案页内可改**：改后自动写 `employee_events(salary_change)`，下次 `recalculate()` 即生效。

#### 5.1.4 头像上传
- 前端：图片 input，客户端压缩到 2MB，类型白名单
- 后端：`<=2MB`，存 `static/avatars/`，DB 存相对路径
- **档案页头像按简历照尺寸展示**（建议 96×120px，3:4 方形圆角）；员工列表行内小头像（如 32-40px）共用同一文件，两处可分别缩放
- 删除：支持重置（删除文件 + 清空 DB 字段）

### 5.2 OA 申请（v6：员工管理平级子页 + 请假并入）

**删除顶级 `nav_oa`**；入职/调岗/离职/请假 4 个流程为**员工管理侧栏下的平级子页**，位于"员工列表"下方（`MODULES.employees.children = ['list','hire','transfer','dismiss','leave']`）。

> **v6 变化**：
> - **请假申请**从待审批页"新建请假"按钮移出，升格为侧栏平级子页"请假申请"（#4）。
> - **员工档案**子页从侧栏移除（#2）——档案由列表点员工进入，不再设独立侧栏入口；保留 `#employees/profile?id=` hash 直达。

| 流程 | 入口 | 提交人 | 审批人 | 效果 |
|------|------|--------|--------|------|
| 入职申请 | 员工管理→入职申请 | 班组长/管理 | 指定审批人（默认你 KEJU） | 生成员工档案（基本信息落库），**审批通过后自动列入员工列表** |
| 调岗申请 | 员工管理→调岗申请 | 管理 | 指定审批人（默认你） | 改部门/岗位/班组，带生效日（不得早于入职日） |
| 离职申请 | 员工管理→离职申请 | 管理 | 指定审批人（默认你） | 置离职+结束日，停止计薪，入离职表 |
| 请假申请 | 员工管理→请假申请 | 班组长/管理 | 指定审批人（默认你） | 事假/年假走审批；病假/调休免审 |

**审批人设定（v6 #9）**：由**超级管理员在后台指定**某人作为审批人（`user_grants` 或新表 `approval_routes`），**不是提交人选择**。未指定时走默认审批流（你 KEJU）。

**自审规则（v6 #3）**：
- `super_admin`（KEJU）：可批准自己提交的事件。
- 其他角色（admin/manager/editor/team_lead/viewer）：自己提交的事件**只能驳回、不能批准**；驳回不受限。
- 实现：`oa_approve_event` 改为"非 super_admin 且 operator_id==当前用户 → 400 拦截"；`oa_reject_event` 保持不限制。

**审批详情查看（v6 #3）**：OA 待审卡片增加"查看详情"展开，展示表单 payload 全部字段（尤其入职申请的完整档案字段），审批人可核对后再批/驳。

**入职申请表单 = 完整员工档案字段**（v5 关键变化 + v6 头像）：与员工档案页可编辑字段一致，含 姓名 / 性别 / 出生日期 / 电话（+255）/ 部门 / 岗位 / 班组 / 技能等级 / 入职日期 / NIDA / NSSF / 银行名称·账号·户名 / 薪资类别 / 薪资基数 / **头像（v6 #6，PNG/JPG ≤2MB）**，均受 §5.1.3 字段类型约束。审批通过后按这些字段自动创建员工记录（含 `employee_id` 分配），出现在员工列表。

**入职提交后跳转（v6 #7）**：入职申请提交成功后，自动切换到 OA 待审 Tab（或跳转审批页），便于发起人确认进入审批流。

**OA 待审/历史**：保留在员工管理顶部 Tabs 区域（"员工列表"Tab 旁边），与申请子页共存。

**审批通知（v6 #8）**：新增顶部**通知铃铛 + 红点**。有"待你审批"事件时红点提示；点开下拉列出待审批事项（标题/提交人/时间），点击可跳转审批。数据源 `GET /api/oa/pending/count` 或新端点。

所有流程产出**带 `effective_date` 的事件**，系统按月推导员工当月状态，自动驱动计薪。

### 5.3 请假管理

#### 5.3.1 4 类请假规则

| 类型 | 额度 | 审批 | 出勤标记 | 影响计薪 |
|------|------|------|----------|----------|
| **事假** (S) | 不累计，按实际 | 需审批（驳回填原因/转交改派） | S | 是 |
| **年假** (Y) | 每年 1/1 发放 28 天；NSSF+NIDA+满 1 年 | 需审批 | Y | 否（带薪） |
| **调休** (T) | 每月 4 天自动累积 | 免审 | T | 否（带薪） |
| **病假** (B) | 每年 14 天默认（可调） | 免审 | P（视为出勤） | 否 |

#### 5.3.2 病假细节
- 额度：每年 1/1 自动发放 14 天至 `leave_balances`（默认 14，管理员可改 `total_entitled`）
- 免审：直接落档，余额扣减
- 出勤落 `P`：病假当日按 P 出勤，不影响计薪（视为正常出勤）
- **余额管理配置**：管理员可在 **系统→计薪参数** 中修改病假年额度（默认 14），修改即时生效，下次发放/扣减按新额度；也可在**员工档案页"假期余额"卡**中手动调整单员工年假/调休/病假余额（改 `total_entitled` / `used`）。所有修改写审计日志。

#### 5.3.3 申请方式
- **姓名/工号模糊搜**（替代原员工ID 输入）：前端 typeahead，搜姓名片段或工号
- 员工列表/档案页显示 `id` 列（通讯录账号）
- 自动解析到 `employee_id` 后提交

#### 5.3.4 出勤标记取值汇总
S（事假）/ Y（年假）/ T（调休）/ P（病假视为出勤）— 与现有 C（破碎）、A（旷工）、L（请假）不冲突。

### 5.4 考勤

- **删除登录前月份选择器**：登录后默认进入"当月数据台"，月份切换仅在顶部 period-bar
- 月度网格保留：手动 P/A/L 纠错；请假自动落 S/Y/T/P
- **出勤收集表**（花名册点选模式）：处理井下/钻工/破碎以外部门员工
  - **按部门选择**（v5）：顶部部门下拉（列出来井下/钻工/破碎以外的所有部门），选定后花名册只显示该部门员工，也可"全部"
  - 选日期 → 按部门过滤花名册 → 点选/改 P/A/L/S/Y/T/P(病) → 批量提交（**不含"驾驶"勾选，v5 已移除**）
  - 写入 `attendance_overrides`
- 修复顶部 period-bar 上下月按钮无反应 bug（P6 修复）

### 5.5 数据采集（取代旧 nav_production）

**改名**：`产量采集` → `数据采集`（中英文 i18n 同步）；模块 ID `nav_production` → `nav_collection`。

**统一结构**：每张表单页 = 上半新提交区 + 下半历史表（日期/类型/提交人/状态）。点历史行可进入再编辑态，提交后写 `collection_history` 旧版本（不覆盖原始）。

#### 5.5.1 井下出渣采集
**单表单提交覆盖 Day + Night 两段**（贴合参考图 `产量统计表单参考.png`）：

| 段 | 字段 |
|----|------|
| 01 | Date（YYYY-MM-DD） |
| 02 | Day shift：**三列带标签** NICKEL(H) / NICKEL(L) / MAWE（数字，v5 补齐列头） |
| 03 | Day 出勤人员：点 `+` 弹层，列出井下部门全员，**多选** → 确认；弹层内**每人带"驾驶"勾选**（v5，仅司机名单内可勾） |
| 04 | Day Remark（文本，可选，**v5 独立**） |
| 05 | Night shift：**三列带标签** NICKEL(H) / NICKEL(L) / MAWE |
| 06 | Night 出勤人员：同上（弹层内同样可勾"驾驶"） |
| 07 | Night Remark（文本，可选，**v5 独立**） |

- 一条 `collection_submissions` 记录 = 一次完整日产
- 写出 `production_records`（D 与 N 各一条），A/L 员工从分配排除
- **驾驶勾选**：弹层内每人一个复选框，仅 `driver_roster` 内人员可选；勾选后该员工当日该班次计司机津贴（单价沿用既有默认 5,000/班，与出勤状态独立）

#### 5.5.2 钻工组采集
**单表单含 N 个钻工队伍槽位**（默认 3，可加减；贴合参考图 `钻工产量统计表单参考.png`）：

每个槽位：
- 队长 select（**v5：只列队长名单内人员**，见 §5.8 计薪参数-钻工队长名单；当前 3 人：BARAKA LAIZER / JOHN BOAY BURA / SHEDRACK PINIEL LAIZER）
- 成员：点 `+` 弹层，列出**该队长**组员，**多选** → 确认
- NICKEL(H) / NICKEL(L) / MAWE 三列产量

支持增删槽位（每槽独立队长+成员+产量）。

**"添加队伍"流程（v5）**：按钮由"添加队长"改名"**添加队伍**"。点击后**先弹人员选择**（仅队长名单内人员可选）→ 选定某人作为队长 → 以该队长名义**生成新队伍槽位**（队长预填、其余字段同默认 3 队逻辑正常录入）→ 后续提交走同一数据流。

> 默认 3 个槽位对应名单内 3 个队长；名单可在计薪参数页维护，新增队长后"添加队伍"可选名单新增项。

#### 5.5.3 破碎计件采集
**独立表单**（贴合参考图 `破碎统计表单参考.png`）：

| 段 | 字段 |
|----|------|
| 01 | Date（YYYY-MM-DD） |
| 02 | Bags 数量（数字） |
| 03 | 出勤人员：点 `+` 弹分拣破碎部门全员，**多选** → 确认 |

#### 5.5.4 出勤收集表（**新增第 4 张**）
- 花名册点选模式，处理井下/钻工/破碎以外部门员工
- 选日期 → **选部门（v5）** → 花名册点选 → 设 P/A/L/S/Y/T/P(病) → 提交（**无"驾驶"勾选，v5 移除**）
- 与 §5.4 出勤网格协同，不重复

#### 5.5.5 提交页 + 历史区 + 再编辑
- **上半**：新提交表单（按子表单类型）
- **下半**：历史表，列 日期/类型/提交人/状态
- 点击历史行 → 进入编辑态（与新提交同表单，但字段预填）
- 提交后：
  - 原始 `collection_submissions` 记录保留（追加版本号）
  - 旧版本写 `collection_history`（不覆盖）
- 编辑权限：editor+，且仅本人提交或 admin+ 可改（审计留痕）

#### 5.5.6 业务规则（沿用 v3）
- 标记 A/L 的员工从当日计件分配排除
- 总额守恒：剩余人员平分当日计件总额
- 极端情况：队长 A/L 且无队员 → 当日计件无人领取（属已知数据边界，见 §13.2）

### 5.6 评分模型（重设计）

#### 5.6.1 班组
- 班组 1 = **LAMBA LAMBA**
- 班组 2 = **SAKA SAKA**
- 班组名存 `employee_groups.name`，DB 可改名（不改业务逻辑）

#### 5.6.2 月份
- 评分卡录入月份由**顶层 period-bar** 决定
- 录入页不再二级选择月份

#### 5.6.3 数据准备与录入流程
- 进入评分卡录入页 → 选班组 → **自动列出该组全员**（按 `employees.custom_number` 升序）
- 列出每个员工的 6 维评分卡（一张卡一人）
- 翻卡操作：一键"上一张/下一张"
- 一张卡内容：
  - 姓名 + 自定义工号（如 W1, W2）
  - 6 维评分：开工 / 努力 / 纪律 / 协作 / 安全 / 驾驶（1-5 分）
  - 总分（自动求和或均分，待评分模型细则定）
  - 备注
- 6 维沿用 P3 已决（参考卡 `评分卡/8月第一周第二组/01.jpeg` 6 维同 PRD 5.6）

#### 5.6.4 自定义工号
- `employees.custom_number TEXT`（如 W1, W2, ABEID1）
- 与唯一 `employee_id`（通讯录账号）区分
- 班组内排序依据

#### 5.6.5 班组与员工档案联动
- `employees.team_id INTEGER` 关联 `employee_groups`
- 员工档案页可改班组
- 改班组后自动重排评分卡显示顺序

#### 5.6.6 计薪联动（沿用 v3）
- 6 维评分 → 去极值 + 管理 1.5 票 → 行为分 → 系数
- 客观层 R1/R2 → S → 发放比例
- 三层奖金模型 → 并入净额
- 三闸面板前端展示（防串通）

### 5.7 薪资计算与展示

#### 5.7.1 计算引擎（沿用 v3）
- 5 轨（井下计件/钻工/破碎/日薪/月薪）+ 评分模型统一
- 双路径核对保留（产量×单价 vs 实际分配；|diff|≤10 视为舍入）

#### 5.7.2 薪资总表（v4 新增列与重命名）
**新增列**：
- `driver_allowance`（司机津贴）— 由**井下出渣采集**勾选"驾驶"自动触发（详见 §5.9）

**重命名列**：
- "井下计件" 列 → **"生产薪资"**（2026-08-12 定稿，P11 实施阶段生效）

**其他列**（沿用 v3）：钻工计件、破碎计件、日薪、月薪、奖金、预支、NSSF、罚款、净额。

净额公式：
```
税前 = 井下 + 钻工 + 破碎 + 日薪 + 月薪
净额 = 税前 + 评分奖金 + 司机津贴 - 预支 - NSSF(10%) - 罚款
```

### 5.8 系统管理

- **用户与权限**（沿用 P4 细粒度）
- **表单自定义**（沿用 P4 schema 驱动）
- **删除源文件管理 UI**：系统→源文件管理页删除
  - **保留 API**：`/upload-source`、`/download-source/<file_type>`、`/api/source/*`（批量导入过渡）
  - **保留后端**：`parser.py`、`scan_source_files()`、`auto_load_source()` 调用链（仍可触发）
  - **UI 隐藏**：系统侧栏不展示源文件管理入口
- **计薪参数**（沿用）：单价/NSSF/病假年额度等通过 `settings` + `/config` 管理
- **计薪参数页（v5 调整）**：
  - **删除**："司机津贴"卡片（全局手工金额 `driver_allowance`）——不再在计算参数配置中显示，也不随 saveConfig/resetConfig 提交
  - **新增**："钻工队长名单"维护（DB 表 `driller_captains`，当前 3 人：BARAKA LAIZER / JOHN BOAY BURA / SHEDRACK PINIEL LAIZER）——钻工组采集的队长下拉与"添加队伍"弹层只列名单内人员
  - **司机津贴单价**保持既有逻辑（默认 5,000/班），不新增配置项（v5 #10 笔误已确认）

### 5.9 司机津贴

**删除**：旧 `_apply_driver_allowance()`（按部门名/岗位含"司机"自动匹配逻辑）。

**保留**：
- `driver_roster` 司机白名单
- **井下出渣采集弹层内勾选"驾驶"**（v5 明确位置，仅名单内允许；不在出勤收集页勾选）
- 按班自动计津贴，**单价沿用既有默认 5,000/班** → 流入薪资总表 `driver_allowance` 列

**删除**：原"司机津贴独立设置"配置入口（统一由出勤勾选 + 名单管控）。

**司机津贴仅产生于井下出渣工人**（C6 沿用），其他部门不涉及。

---

## 6. 角色与权限模型

- **预设角色**：`super_admin`(KEJU) > `admin`(中方管理) > `manager`(本地主管) > `team_lead`(班组长) > `viewer`(只读)。
- **细粒度**：权限 = **(模块, 动作, 数据范围)** 三元组；用户除继承角色外，可**单独增删**某项权限。
- **数据范围**：全员 / 指定班组（班组长仅本组）。
- 典型映射：
  - 班组长 = 数据采集 + 出勤收集 + 评分录入（本组）、请假代提、不能见他组
  - 管理 = 全矿录入 + 查看
  - 你 = 全部 + 审批 + 配置

---

## 7. 表单引擎（明确字段类型）

**不做**可视化拖拽布局生成器；**采用 schema 驱动**。

#### 7.1 标准字段类型清单

| 类型 | UI 控件 | 校验 |
|------|---------|------|
| `text` | input | 长度限制 |
| `textarea` | 多行 input | 长度限制 |
| `number` | input | 数值范围 |
| `date` | datepicker | YYYY-MM-DD |
| `phone` | input | 默认 `+255` 前缀；仅数字 |
| `select` | 下拉 | 枚举值 |
| `enum` | 下拉 | 枚举值（业务可配置） |
| `file` | 上传组件 | image/png/jpeg；≤2MB |
| `employee_picker` | 模糊搜 | 姓名/工号 → employee_id |

#### 7.2 字段落表策略
- 标准字段落独立列
- 自定义字段存 `custom_fields` JSON 列
- 入职表单字段 = 员工基本信息字段，亦走此机制

---

## 8. 概念数据模型

### 8.1 员工主档（employees — 扩展现有表）

| 列名 | 类型 | 变化 | 说明 |
|------|------|------|------|
| `id` | TEXT PK | | 员工 ID（通讯录账号, 如 `111`） |
| `name` | TEXT | | 显示名 |
| `department` | TEXT | | 部门 |
| `position` | TEXT | v3 新加 | 岗位 |
| `skill_level` | TEXT | v3 新加 | 技能等级 |
| `hire_date` | TEXT | v3 新加 | 入职日期 YYYY-MM-DD |
| `nida_number` | TEXT | v3 新加 | NIDA 证件号 |
| `nssf_number` | TEXT | v3 新加 | NSSF 社保号 |
| `phone` | TEXT | | 电话（默认 +255） |
| `bank_name` | TEXT | v3 新加 | 银行名称 |
| `bank_account` | TEXT | v3 新加 | 银行账号 |
| `bank_owner` | TEXT | v3 新加 | 户名 |
| `status` | TEXT | v3 新加 | active/dismissed/suspended |
| `dismissed_at` | TEXT | v3 新加 | 离职日期 |
| **`gender`** | TEXT | **v4 新加** | 男/女/其他 |
| **`date_of_birth`** | TEXT | **v4 新加** | 出生日期 YYYY-MM-DD（年龄=计算列） |
| **`avatar_path`** | TEXT | **v4 新加** | 头像相对路径 |
| **`custom_number`** | TEXT | **v4 新加** | 自定义工号（如 W1, W2） |
| **`team_id`** | INTEGER | **v4 新加** | 班组 ID（关联 employee_groups） |
| `default_type` | TEXT | | 旧列保留 |
| `day_rate` | INTEGER | | 旧列保留 |
| `monthly_salary` | INTEGER | | 旧列保留 |
| `nssf_enrolled` | INTEGER | | 旧列保留 |
| `note` | TEXT | | 旧列保留 |
| `custom_fields` | TEXT | v3 新加 | JSON 兜底列 |

#### 8.2 数据采集提交（collection_submissions — v4 新表）

```sql
CREATE TABLE IF NOT EXISTS collection_submissions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    form_type TEXT NOT NULL,            -- underground / driller / crush / attendance
    submission_date TEXT NOT NULL,      -- 采集日期 YYYY-MM-DD
    payload TEXT NOT NULL DEFAULT '{}', -- JSON：表单完整数据（含 D/N 产量、桶位、出席名单）
    operator_id TEXT NOT NULL,          -- 提交人
    month TEXT NOT NULL,                -- YYYY-MM（用于快速过滤）
    created_at TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    updated_at TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    version INTEGER NOT NULL DEFAULT 1
);
CREATE INDEX IF NOT EXISTS idx_cs_month ON collection_submissions(month, form_type);
CREATE INDEX IF NOT EXISTS idx_cs_date ON collection_submissions(submission_date);
```

#### 8.3 数据采集编辑历史（collection_history — v4 新表）

```sql
CREATE TABLE IF NOT EXISTS collection_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    submission_id INTEGER NOT NULL,
    version INTEGER NOT NULL,           -- 旧版本号
    payload TEXT NOT NULL,              -- 旧 payload
    operator_id TEXT NOT NULL,          -- 编辑人
    created_at TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    FOREIGN KEY (submission_id) REFERENCES collection_submissions(id)
);
```

#### 8.4 班组表（employee_groups — v4 新表）

```sql
CREATE TABLE IF NOT EXISTS employee_groups (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE,          -- LAMBA LAMBA / SAKA SAKA
    description TEXT DEFAULT '',
    created_at TEXT NOT NULL DEFAULT (datetime('now','localtime'))
);
```

#### 8.5 钻工队长名单（driller_captains — v5 新表）

```sql
CREATE TABLE IF NOT EXISTS driller_captains (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    employee_id TEXT NOT NULL UNIQUE,   -- 关联 employees.id（通讯录账号）
    name TEXT NOT NULL,                 -- 队长姓名
    sort_order INTEGER DEFAULT 0,       -- 展示顺序
    created_at TEXT NOT NULL DEFAULT (datetime('now','localtime'))
);
```

- 初始种入 3 人：BARAKA LAIZER / JOHN BOAY BURA / SHEDRACK PINIEL LAIZER
- 在计薪参数页维护（增删、改名、排序）
- 钻工组采集队长下拉与"添加队伍"弹层仅列此名单

#### 8.6 其他表（沿用 v3/v4）
- `employee_events`、`overrides`、`attendance_overrides`、`settings`、`monthly_data`、`audit_log`、`shift_additions`、`driller_additions`、`bonus_penalties`、`dismissed_employees`、`admin_users`、`leave_balances`、`leave_requests`、`driver_roster`、`scoring_cards/entries`、`objective_records`、`permissions`、`user_grants`、`form_schemas/fields`、`collection_submissions`、`collection_history`、`employee_groups`

#### 8.7 表变化摘要（v3 → v4 → v5）
- **v4**：employees +5 列（gender / date_of_birth / avatar_path / custom_number / team_id）；新增 `collection_submissions` / `collection_history` / `employee_groups`；`leave_requests` 新增 `sick`、`leave_balances` 新增病假额度
- **v5**：新增 `driller_captains`（钻工队长名单）；`settings` **移除** `driver_allowance`（旧全局手工津贴）在计薪参数页的读写

---

## 9. 非功能需求

- **移动优先**：班组长录入页手机优先、大按钮、中英切换。
- **i18n**：中/英双语（无斯语），所有新增页面文案同步进 `static/js/i18n.js`。
- **UX**：月份切换顺滑（period-bar + 懒加载）；档案为独立子页、采集为卡片式录入；选人弹层/选中框统一样式对齐。
- **输出**：工资条、评分公示卡支持打印/导出（现金无账号）。
- **演进式**：在现有 Flask + SQLite + SPA 底座上扩展，不推倒重写。

---

## 10. 迁移策略

- 新系统启用后，**旧 `kilwa.db` 冻结为只读归档**（`archived_kilwa`），不事件化、不回溯。
- Excel 上传保留为批量导入通道（过渡期纸质→Excel→导入仍可用），UI 隐藏入口。
- 新数据全从网页录入。
- 班组初始化：种入 LAMBA LAMBA、SAKA SAKA 两组（id=1, 2），并把现有井下出渣工人按 `custom_number` 顺序排入班组 1。
- **v5 初始化**：`driller_captains` 种入 3 名钻工队长（BARAKA LAIZER / JOHN BOAY BURA / SHEDRACK PINIEL LAIZER）。

---

## 11. 实施路线（分阶段，每阶段可独立上线）

| 阶段 | 目标 | 完成判据 |
|------|------|----------|
| P0 | 需求定稿（v3 PRD） + 数据模型/接口契约 + 前端框架/导航层级重建 | v3 通过、表结构定稿、新 IA 可跑 |
| P1 | 员工档案 + OA 生命周期事件化 | 入职→审批→计薪自动驱动 |
| P2 | 考勤网格重构 + 请假（事假审批 + 年假/调休余额）+ 产量 Web 录入 + 月份切换打磨 | 三类请假/产量按验收标准通过 |
| P3 | 评分模型并入 | 双路径核对 0 偏差 |
| P4 | 角色细化 + 表单自定义 + 手机端 + 全局查询 | 权限/表单按验收标准通过 |
| P5 | 旧数据归档 + Excel 导入收尾 + 本地自测 | 旧库只读、新数据全网页录入 |
| **P6** | **顶部交互清理** | 登录前月份选择器删除 / 数据台月份切换 bug 修复 / 搜索简化（删 scope） / 员工页合并（删档案页） |
| **P7** | **员工档案字段扩展** | 性别 / 年龄 / 头像上传 / 字段类型校验 / 电话 +255 / 薪资类别+基数在档案页可改 |
| **P8** | **OA 合并入员工 + 请假增加病假** | 3 流程嵌入员工管理底部 / 病假免审+14 天额度 / 请假申请改姓名索引 / 员工列表显示 id |
| **P9** | **数据采集模块重设计** | 4 表单（井下/钻工/破碎/出勤收集）+ 提交页 + 历史区 + 再编辑 + D+N 同表 |
| **P10** | **评分模块重设计** | 班组 LAMBA/SAKA + 自定义工号 + 顶层月份 + 选班组自动出全员 + 一张张卡录入 |
| **P11** | **系统清理 + 薪资总表新列** | 源文件管理 UI 删除 / 司机津贴独立设置删除 / 薪资总表增 driver_allowance 列 / 井下计件列改名"生产薪资" |
| **P12** | **v5 需求对齐迭代** | 见下表（§0.1 的 10 条有效反馈） |

**P12 细分**（对应 §0.1 编号）：

| 子项 | 内容 | 验收 |
|------|------|------|
| P12.1 | OA 三申请升格为员工管理平级子页；入职申请=完整档案表单，审批后自动入列表 | 侧栏可见 3 个子页；入职审批通过后员工出现在列表 |
| P12.2 | 员工档案独立完整页（非弹窗）+ 简历尺寸头像 | `#employees/profile?id=` 独立页；头像 3:4 简历尺寸 |
| P12.3 | 档案页三类假期余额显示且可修改（年假/调休/病假） | 修改余额落库、写审计 |
| P12.4 | 员工姓名可选中复制，点击才跳转 | 鼠标选中不跳转；点击跳档案页 |
| P12.5 | 井下出渣采集：三列标签 + Day/Night 双备注 + 弹层内驾驶勾选 | 三列头可见；两段各有 Remark；名单内可勾驾驶 |
| P12.6 | 钻工队长名单化 + "添加队伍"先选队长再生成槽位 | 队长下拉只列名单 3 人；添加队伍流程成立 |
| P12.7 | 出勤收集按部门选择提交；移除驾驶勾选 | 部门下拉过滤花名册；无驾驶列 |
| P12.8 | 选人弹层/选中框统一样式对齐 | 全采集表单选中框对齐一致 |
| P12.9 | 计薪参数页删除旧司机津贴项；新增钻工队长名单维护 | 页面无全局司机津贴；队长名单可维护 |
| P12.10 | 评分模块改名"评分系统"，补 i18n 中/英键 | 侧栏显示"评分系统"，无原始键名 |
| **P13** | **v6 需求对齐迭代** | 见下表（§0.2 的 10 条有效反馈） |

**P13 细分**（对应 §0.2 编号）：

| 子项 | 内容 | 验收 |
|------|------|------|
| P13.1 | 员工列表部门筛选修复 | 选部门后筛选生效且选中值保留 |
| P13.2 | 员工档案子页移出侧栏 | 侧栏无"员工档案"入口；`#employees/profile?id=` 仍可直达 |
| P13.3 | OA 自审规则分角色 + 审批详情展开 | super_admin 可自批；其他角色自提交仅可驳；卡片可展开看 payload |
| P13.4 | 请假申请升格侧栏子页 | 侧栏"员工管理"下有请假申请子页；待审批页无"新建请假"按钮 |
| P13.5 | 侧栏折叠底部区样式优化 | 折叠态中英切换/登录/改密/退出紧凑对齐 |
| P13.6 | 入职表单加头像上传 | 入职表单含头像上传，PNG/JPG ≤2MB |
| P13.7 | 入职提交后自动进待审批页 | 提交成功跳转 OA 待审 Tab |
| P13.8 | 顶部通知铃铛/红点 | 待审批时红点；点开列出待审批事项，可跳转 |
| P13.9 | 超级管理员后台指定审批人 | 后台可设指定审批人；未指定走默认流 |
| P13.10 | 全页面中英双语审查 | 页面无硬编码中文；中英切换完整 |

每个阶段在本地可独立验证；是否"上线"由部署时机（见 `DEV_WORKFLOW.md` §10）决定。

---

## 12. 已决事项

### v3 已决（保留）
1. 基本信息最小集起步，已含银行字段，后续表单引擎扩展
2. 事假审批：需审批，支持 **批准 / 驳回（填原因）/ 转交（改派）**
3. 请假规则（v3 定稿）：年假需审批/28 天/NSSF+NIDA+满 1 年；调休免审/月 4 天
4. 评分模型：P3（先解生命周期 + 采集痛点）
5. 导航重构：P0 地基
6. 表单引擎：schema 驱动，不做拖拽生成器；预置 6 张表单
7. 产量采集：出勤与产量分离
8. 司机津贴：driver_roster 管控 + 出勤勾选触发

### v4 新增决议
- **D9**：登录前月份选择器取消（point 1）
- **D10**：搜索简化为单一全局入口，删除 scope 下拉（point 3）
- **D11**：员工档案页删除，详情并入列表抽屉（point 4）
- **D12**：employees 表加 `gender / date_of_birth / avatar_path / custom_number / team_id`（points 5,6,15）
- **D13**：员工头像上限 2MB，仅 image/png/jpeg（point 6）
- **D14**：电话字段默认 `+255` 前缀（point 6）
- **D15**：字段类型严格化（point 6, C7）
- **D16**：薪资类别 + 基数可在员工档案抽屉直接修改（point 6）
- **D17**：OA 三流程（入职/调岗/离职）嵌入员工管理页底部 3 子页；原 OA 待审/历史入口移入员工管理顶部 Tabs（point 7）
- **D18**：请假类型新增"病假"，免审，14 天/年默认余额可调，出勤落 P（point 8）
- **D19**：请假申请改"姓名/工号"模糊搜；员工列表显示 id 列（point 8）
- **D20**：`产量采集` → `数据采集`（中英文 i18n 同步改名），模块 ID `nav_production` → `nav_collection`（point 9）
- **D21**：井下/钻工表单含 Day + Night 双段（point 9,10）
- **D22**：钻工表单支持 N 队长槽位，可加减（point 10）
- **D23**：数据采集 4 表单（井下/钻工/破碎/出勤收集）（points 9,11,12）
- **D24**：数据采集表单下半分屏"提交页 + 历史区 + 再编辑"（point 13）
- **D25**：评分班组 1 = LAMBA LAMBA，班组 2 = SAKA SAKA，DB 存 `employee_groups.name` 可改名（point 14）
- **D26**：评分月份由顶层 period-bar 决定，录入页不再二级选择（point 14）
- **D27**：选班组自动出全员（按 `custom_number` 升序），一张卡一人录入（point 15）
- **D28**：删除源文件管理 UI（保留 API 作批量导入过渡）（point 16）
- **D29**：删除 `_apply_driver_allowance()` 旧逻辑；薪资总表增 `driver_allowance` 列；**"井下计件"列改名"生产薪资"**（point 17，2026-08-12 定稿）

### v5 新增决议（2026-08-12 验收后反馈）

- **D30**：OA 三申请（入职/调岗/离职）从员工列表页底部按钮**升格为员工管理侧栏平级子页**（`employees.children = ['list','hire','transfer','dismiss']`）；入职申请表单 = **完整员工档案字段**，审批通过后自动列入员工列表（v5 #1）
- **D31**：员工档案从弹窗**恢复为独立完整页面**（`#employees/profile?id=`），头像按**简历照尺寸**（96×120px，3:4 方形圆角）展示（v5 #2）
- **D32**：档案页显示**年假/调休/病假三类余额且均可修改**，修改写 `leave_balances` + 审计日志（v5 #3）
- **D33**：员工列表姓名**可选中复制**（不触发跳转），点击姓名/行才进入档案页（v5 #5a）
- **D34**：井下出渣采集**补齐 NICKEL(H)/(L)/MAWE 三列标签**；**Day 与 Night 各自独立 Remark**（v5 #4）
- **D35**：钻工队长**名单化**（新表 `driller_captains`，初始 3 人，计薪参数页维护）；队长下拉与"添加队伍"弹层**只列名单内人员**；按钮改名"**添加队伍**"，流程 = 先选队长 → 生成队伍槽位（v5 #5b）
- **D36**：评分模块改名"**评分系统**"，补 `nav_scoring` 中/英 i18n 键（v5 #6）
- **D37**："驾驶"勾选从出勤收集**移到井下出渣采集**（Day/Night 选择人员弹层内每人勾选，仅司机名单内可勾）；出勤收集**去掉驾驶**，改为**按部门**过滤花名册后提交（v5 #7）
- **D38**：全采集表单选人弹层/选中框**统一公共样式**（`emp-chips` 等），对齐美化（v5 #8）
- **D39**：计薪参数页**删除旧"司机津贴"卡片**（全局手工金额）及其 saveConfig/resetConfig 字段；**新增"钻工队长名单"维护**（`driller_captains`）；司机津贴单价沿用既有默认 5,000/班，不新增配置项（v5 #9；#10 笔误已确认）

### v6 新增决议（2026-08-12 v5 验收后反馈）

- **D40**：修复员工列表部门筛选不生效——`renderEmployeeTable()` 重建 `empFilterDept` 下拉时保留当前选中值（v6 #1）
- **D41**：员工档案子页从侧栏移除（`MODULES.employees.children` 去掉 `'profile'`），保留 `SUBPAGE_TARGETS['employees/profile']` 映射与 hash 直达（v6 #2）
- **D42**：OA 自审规则分角色——`super_admin` 可批准自己提交；其他角色自己提交只能驳回不能批准；`oa_approve_event` 改为非 super_admin 且 operator_id==当前用户时 400（v6 #3）
- **D43**：OA 待审卡片支持**展开查看表单详情**（payload 全字段，尤其入职 18 字段）（v6 #3）
- **D44**：请假申请从待审批页移出，升格为侧栏平级子页"请假申请"（v6 #4）
- **D45**：优化侧栏折叠态底部区（中英切换/登录/改密/退出）样式（v6 #5）
- **D46**：入职申请表单加入头像上传（PNG/JPG ≤2MB，同档案页机制）（v6 #6）
- **D47**：入职申请提交成功后自动跳转 OA 待审 Tab（v6 #7）
- **D48**：新增顶部站内通知铃铛/红点——有"待你审批"事项时红点提示，点开列出待审批事项可跳转（v6 #8）
- **D49**：审批人由**超级管理员后台指定**（非提交人选择），未指定走默认审批流（v6 #9）
- **D50**：全页面字段中英双语审查——消灭硬编码中文（当前 113 处），所有展示字段补 i18n 键（v6 #10）

---

## 13. 验收标准（Acceptance Criteria）

### 13.1 功能验收（v4+v5 增量，原 v3 验收保留）

| 域 | 验收项 |
|---|---|
| 顶部交互 | 登录后不弹月份选择器；数据台 period-bar 上下月按钮可点；搜索无 scope 下拉，输入即时全局搜 |
| 员工页 | **v5**：无独立档案页弹窗；员工列表行/姓名**点击** → 独立档案页；姓名**可选中复制**（选中不跳转） |
| 员工列表 | 显 ID 列；显示性别/年龄/班组/薪资类别/基数 |
| 员工档案 | **v5**：独立完整页面（`#employees/profile?id=`）可编辑性别/出生日期/头像/电话+255/薪资类别+基数；头像**简历照尺寸**（96×120px，3:4 方形圆角） |
| 假期余额 | **v5**：档案页显示年假/调休/病假三类余额，**每类均可调整**（改 total_entitled/used，写审计） |
| 头像 | 上传 PNG/JPG ≤2MB；预览可见；持久化 `static/avatars/<id>.<ext>` |
| 字段类型 | 日期字段不接受文本；电话自动补 +255；部门/薪资类别=枚举 |
| OA | **v5**：入职/调岗/离职为员工管理侧栏 3 个平级子页；入职申请=完整档案表单，审批通过后自动入列表；原 OA 待审/历史保留在员工管理 Tabs |
| 请假 | 4 类（事假/病假/年假/调休）；病假免审，14 天额度，出勤落 P |
| 请假申请 | 姓名/工号模糊搜自动解析；员工列表显示 id |
| 数据采集 | 4 表单可访问（井下/钻工/破碎/出勤收集）；井下/钻工表单含 Day+Night 双段；破碎含 Bags + 出勤 |
| 井下出渣 | **v5**：三列带标签 NICKEL(H)/(L)/MAWE；Day 与 Night **各自独立 Remark**；Day/Night 选人弹层内每人带"驾驶"勾选（仅司机名单内可勾） |
| 钻工采集 | **v5**：队长下拉**只列 `driller_captains` 名单内人员**（当前 3 人）；"添加队伍"先选队长再生成槽位；成员弹层列该队长组员 |
| 出勤收集 | **v5**：按部门下拉过滤花名册后提交；**无"驾驶"勾选** |
| 采集样式 | **v5**：选人弹层/选中框统一公共样式，全表单对齐一致 |
| 数据采集历史 | 下半分屏历史表；点行再编辑；编辑写 `collection_history` |
| 评分 | 班组名 LAMBA LAMBA / SAKA SAKA；月份由顶层决定；选班组自动出全员按工号排序；一张张卡录入含 6 维 + 总分 |
| 评分命名 | **v5**：侧栏模块改名"**评分系统**"（nav_scoring i18n 中/英已补，不显示原始键名） |
| 系统 | 无源文件管理入口；批量导入 API 仍可用 |
| 司机津贴 | 无独立设置入口；仅 driver_roster + 井下采集勾选；自动流入 `driver_allowance` |
| 计薪参数 | **v5**：页面**无旧"司机津贴"卡片**；新增"钻工队长名单"维护；司机津贴单价沿用默认 5,000/班 |
| 薪资总表 | 含 `driver_allowance` 列；井下计件列已改名"生产薪资" |
| **部门筛选** | **v6**：员工列表选部门后筛选生效，选中值保留不重置 |
| **侧栏子页** | **v6**：员工档案子页不再出现在侧栏；`#employees/profile?id=` 仍可直达 |
| **OA 自审** | **v6**：super_admin 可批准自己提交；其他角色自己提交只能驳回不能批准 |
| **审批详情** | **v6**：OA 待审卡片可展开查看表单 payload 全字段（含入职 18 字段） |
| **请假子页** | **v6**：请假申请为侧栏平级子页；待审批页无"新建请假"按钮 |
| **侧栏折叠** | **v6**：折叠态底部区（中英/登录/改密/退出）紧凑对齐 |
| **入职头像** | **v6**：入职表单支持头像上传（PNG/JPG ≤2MB） |
| **入职跳转** | **v6**：入职提交成功后自动进入待审批页 |
| **通知** | **v6**：顶部铃铛红点；待审批事项红点提示，点开可跳转 |
| **审批人设定** | **v6**：超级管理员后台可指定审批人；未指定走默认流 |
| **中英双语** | **v6**：全页面展示字段无硬编码中文；中英切换完整 |

### 13.2 边界与异常验收（v4+v5 增量）
- 头像超 2MB / 非 PNG/JPG → 拒绝上传
- 日期字段填文本 → 前端立即报错，不静默
- 病假余额不足 → 提交被拦或强提醒
- 班组改名后既有评分卡按新班组名展示
- 队长 A/L 且无队员 → 当日计件无人领取（已知数据边界，与 v3 同）
- 数据采集历史再编辑权限：非本人 + 非 admin → 拒绝
- **v5**：驾驶勾选不在司机名单内 → 前端禁勾并提示（后端二次校验拦截）
- **v5**：出勤收集未选部门 → 默认展示"全部非井下/钻工/破碎"员工，提交不因未选部门而失败
- **v5**：入职申请审批通过时必填字段缺失 → 驳回并提示缺项，不落库
- **v5**：`driller_captains` 名单为空 → 钻工采集队长下拉显示空态提示"请先在计薪参数维护队长"
- **v6**：非 super_admin 自审批准 → 后端 400 拦截"不能批准自己提交的事件"；自审驳回不受限
- **v6**：未指定审批人的事件 → 走默认审批流（KEJU），不因缺审批人挂起
- **v6**：通知红点仅在存在"待你审批"事项时显示；无权限用户不显示铃铛
- **v6**：中英切换后残留硬编码中文 → 视为 i18n 缺陷，验收拦截

### 13.3 非功能验收
- i18n：中英切换下全部新增页面文案完整无遗漏
- 移动端：班组长录入页在手机视口可用
- 性能：月份切换/全局查询体感顺滑
- 演进：在现有 Flask+SQLite 底座扩展，不推倒重写

---

## 14. 关键用户流程（User Flows）

> 标注格式：**[阶段] · 主要角色 · 前置 → 后置**

### v3 流程（保留 F0-F13）
- F0 系统初始化 · F1 入职 OA · F2 调岗/薪资变 · F3 事假 · F3b 年假 · F4 调休 · F5 出勤收集 · F6 产量录入 · F7 司机津贴 · F8 评分 · F9 离职 · F10 月度关账 · F11 表单自定义 · F12 权限异常 · F13 权限调整

### v4 新增流程

**F14 员工请假（含病假）** [P8] · 班组长/员工 · 登录 → 出勤落 S/Y/T/P
- 打开请假页 → 输入姓名/工号 → 自动索引到 employee_id → 选请假类型（事假/病假/年假/调休） → 填日期/时长
- 病假：自动通过 → 出勤落 P（视为出勤） → 余额扣减
- 事假/年假：走审批流（待审 → 批准/驳回/转交）
- 调休：免审 → 出勤落 T → 余额扣减

**F15 数据采集（井下）** [P9/v5-P12.5] · 班组长 · 登录 → 直写 DB
- 数据采集 → 井下 → 填 Date → Day shift **三列带标签** NICKEL(H)/(L)/MAWE 产量 + Day 出勤（点 `+` 弹层多选井下部门员工，**每人可勾"驾驶"，仅司机名单内**） → **Day Remark** → Night shift 三列 + Night 出勤（同上） → **Night Remark** → 提交
- 系统写 `collection_submissions` + 派生 `production_records`（D 与 N 各一条）；勾选驾驶者按既有默认 5,000/班计班津贴

**F15b 数据采集（钻工）** [P9/v5-P12.6] · 班组长 · 登录 → 直写 DB
- 数据采集 → 钻工 → 默认 3 队伍槽位（对应 `driller_captains` 名单 3 人） → 每槽队长下拉（**只列名单内人员**） → 加成员（点 `+` 弹该队长组员） → 填三列产量 → 提交
- **添加队伍**：点"添加队伍" → 先弹人员选择（仅名单内队长） → 选定队长 → 生成新队伍槽位（队长预填，与默认 3 队同一数据流） → 提交

**F15c 数据采集（破碎）** [P9] · 班组长 · 登录 → 直写 DB
- 数据采集 → 破碎 → 填 Date + Bags + 出勤（点 `+` 弹分拣破碎部门） → 提交

**F15d 数据采集（出勤收集）** [P9/v5-P12.7] · 部门负责人 · 登录 → 直写 DB
- 数据采集 → 出勤收集 → 选日期 → **选部门（井下/钻工/破碎以外部门下拉，可全部）** → 花名册点选员工 → 设 P/A/L/S/Y/T/P(病) → 提交（**无驾驶勾选**）

**F16 评分录入** [P10] · 管理/班组长 · 选班组 → 录卡 → 奖金并入净额
- 评分系统（v5 改名，侧栏显示"评分系统"）→ 选班组（LAMBA LAMBA / SAKA SAKA）→ 自动列全员（按 `custom_number` 升序） → 一张张卡录 6 维 + 总分 + 备注 → 翻卡（上/下）→ 全部完成后提交
- 系统写 `scoring_entries` + 后续 P3 评分汇总与三闸面板沿用

**F17 数据采集历史再编辑** [P9] · 编辑者 · 登录 → 修改历史
- 数据采集 → 选子表单 → 下半屏历史表 → 点行 → 进入编辑态 → 修改字段 → 提交
- 系统：旧 `collection_submissions` 版本号 +1；旧 payload 写 `collection_history`；审计留痕
- 权限：editor+；且仅本人提交或 admin+ 可改

**F18 头像上传** [P7] · 管理 · 登录 → 头像入库
- 员工列表 → 点击姓名 → 独立档案页 → 点头像区 → 选择 PNG/JPG（≤2MB） → 上传 → **简历尺寸预览** → 持久化 `static/avatars/<id>.<ext>`

### v5 新增流程

**F19 员工档案（独立页）** [v5-P12.2/3] · 管理/班组长 · 列表 → 档案
- 员工列表 → **点击**姓名/行 → 进入 `#employees/profile?id=` 独立页 → 查看/编辑基本信息、头像、薪资类别+基数 → 时间线 → 请假记录 → **三类假期余额（年假/调休/病假）逐类"调整"** → 修改写 `leave_balances` + 审计
- 姓名文字**可鼠标选中复制**（选中不触发跳转）

**F20 OA 申请（子页）** [v5-P12.1] · 班组长/管理 · 侧栏 → 提交 → 审批 → 生效
- 员工管理 → 侧栏"入职申请" → **完整档案表单**（全部基本信息字段） → 提交 → 你审批 → 通过后自动创建员工并**出现在员工列表**（含 employee_id 分配）
- 调岗申请/离职申请 同路径：侧栏子页 → 搜索员工 → 填新部门/岗位/生效日（或离职日+原因） → 审批 → 事件落库驱动计薪

**F21 计薪参数维护（v5）** [v5-P12.9] · admin · 系统 → 计薪参数
- 系统 → 计薪参数 → 配置单价/NSSF/病假额度/井下计薪模式
- **钻工队长名单**可增删改（同步 `driller_captains`）；**司机津贴单价沿用默认 5,000/班**（不新增配置项）
- 页面**不再显示**旧的全局"司机津贴"金额输入

### v6 新增流程

**F22 部门筛选** [v6-P13.1] · 登录用户 · 列表 → 筛选
- 员工列表 → 选部门下拉 → 列表即时过滤且**下拉选中值保留**（不因渲染重置）

**F23 审批详情查看** [v6-P13.3] · 审批人 · 待审 → 核对
- 员工管理 → OA 待审 → 点"查看详情" → 展开该事件 payload 全字段（入职含完整档案字段） → 核对后批准/驳回

**F24 自审规则** [v6-P13.3] · 提交人 · 待审 → 操作
- super_admin 提交的事件：自己可批准/驳回。
- 其他角色提交的事件：自己只能驳回（后端 400 拦截自批），可转他人审批。

**F25 请假申请（子页）** [v6-P13.4] · 班组长/管理 · 侧栏 → 提交
- 员工管理 → 侧栏"请假申请" → 搜姓名/工号 → 选类型（事假/病假/年假/调休）→ 日期/天数 → 提交（病假/调休免审直接落；事假/年假走审批流）

**F26 入职提交跳转** [v6-P13.7] · 班组长/管理 · 子页 → 待审
- 员工管理 → 入职申请 → 填全字段（含头像）→ 提交 → **自动切换到 OA 待审 Tab** 确认进入审批流

**F27 审批通知** [v6-P13.8] · 审批人 · 全局 → 铃铛
- 顶部铃铛：有待审批事项时红点 → 点开列出"待你审批"（类型/提交人/时间）→ 点击跳转 OA 待审

**F28 指定审批人（后台）** [v6-P13.9] · super_admin · 系统 → 设定
- 系统（或员工管理）→ 审批人设定 → 指定某用户为某类事件的审批人 → 提交事件按设定路由审批；未指定走默认流（KEJU）

---

## 15. 成功指标（KPI）

- 顶部交互：登录到数据台 ≤2 步（登录→数据台）；搜索 1 步可达
- 员工操作：所有员工信息修改在独立档案页内完成；姓名可选中复制、点击进档案（1 步）
- 调岗/薪资变：1 次网页操作即生效
- 采集：井下 D+N、钻工多队、破碎、出勤 4 类采集全网页直录，班组长手机可完成
- 评分：选班组 → 录卡 ≤3 步；翻卡流畅无卡顿
- 车辆：仅司机名单内人员可在井下采集勾选驾驶并按班计津贴，乱用可追溯
- 历史可追溯：所有数据采集提交有版本历史，编辑留审计
- 争议：工资条、评分公示卡可打印公示，工人异议有迹可查
- **v6 审批效率**：待审批红点 ≤1 步可达；审批详情展开 ≤1 步
- **v6 双语**：页面切换英文后无残留硬编码中文（113 处清零）
- **v6 自审**：自审规则按角色一致执行，无"能驳不能批"困惑
