# 薪资系统重构 — 需求与架构说明书

> **状态**：v3（PRD 定稿候选，含司机名单机制、验收三分类、用户流程标注；待最终审核）
> **最后更新**：2026-08-12
> **目标读者**：项目 Owner（KEJU）、实施 Agent
> **来源**：多轮需求梳理会话（矿区管理评分系统 + 现有 enprizon-salary 看板系统）

---

## 1. 背景与目标

### 1.1 现状痛点
当前薪资数据在 **3 个地方产生、本系统只"算"不"收"**：

```
企业微信表单(入职/离职/请假) ─┐
纸质记录(入职/调岗/离职)       ─┼─→ 人工整理成 Excel ─→ 本系统上传计算
现场表单(出勤/产量)            ─┘
```

- 生命周期事件（入职/调岗/离职/薪资变）散落在企微 + 纸质，本系统要靠**人工再录一遍**才生效 → 漏录即漏薪（调岗问题即源于此）。
- 评分系统是另一套独立 Excel（井下出渣工人），与本系统 5 轨计薪分家。
- 现有本系统偏"看板"：多个页面是不同维度的仪表盘，平铺 tab、无父子页、月份切换不丝滑。

### 1.2 重构目标
将本系统打造为 **唯一的"采集 + 计算 + 展示"网页平台**，吸收企微与纸质的采集职能，让生命周期事件**自动驱动**薪资，消灭重复录入。

### 1.3 非目标（本期不做）
- 工人自助端（工人完全离线、现金发薪，无账号）。
- 对接企业微信 API（企微全弃用）。
- 斯瓦希里语（仅中英双语）。
- 可视化拖拽表单生成器（采用配置驱动）。

---

## 2. 关键约束（不可妥协的前提）

| # | 约束 | 影响 |
|---|------|------|
| C1 | **现金发薪，工人完全离线，无工人账号** | 全管理端；工人侧输出走打印/导出（工资条、评分公示卡） |
| C2 | **企微全弃用**，网页为唯一采集入口 | Excel 上传降级为"批量导入/过渡"通道，非主路径 |
| C3 | **仅中英双语**（无斯语） | i18n 维持中/英，不增斯语 |
| C4 | **旧数据只读归档**，不回溯、不事件化 | 新系统全新起；旧 `kilwa.db` 冻结为只读镜像 |
| C5 | 井下评分系统 = 当前**井下计件工人**（同一批人） | 其 `salary_type` 可在"计件"↔"评分模型"间切换 |
| C6 | **司机津贴仅井下出渣工人产生**，由出勤勾选"驾驶"触发，且须在校验**司机名单**内方可勾选；自动流入薪资 | 车辆管控（防乱用）+ 津贴精准计发，不再依赖旧 `_apply_driver_allowance` 通用逻辑 |

---

## 3. 范围与扩展性

- **本期范围**：以井下工人（含其评分模型）为首个完整落地场景。
- **扩展性预留**：架构按"可加部门"设计（员工/计薪类型/表单均参数化），将来可纳入其他部门，不预写其他部门逻辑。

---

## 4. 信息架构（导航树）

```
工作台(总览)
├─ 员工管理
│   ├─ 员工列表（搜索/筛选：部门·班组·在职状态）
│   └─ 员工档案（点姓名进入）
│       ├─ 基本信息（录入/编辑）
│       ├─ 生命周期时间线（入职/调岗/离职/薪资变/技能变，可筛选查询）
│       ├─ 请假记录（可查询）
│       └─ 关联数据入口（当月薪资/出勤/产量/评分）
├─ OA 审批
│   ├─ 入职 │ 调岗 │ 离职 │ 薪资/技能变更（提交→审批）
├─ 考勤
│   ├─ 出勤网格（可手动改 P/A/L，含请假类型区分）
│   └─ 请假审批（仅需审批的类型）
├─ 产量采集
│   └─ 井下/钻工/破碎 录入（班组长，仅本组，手机优先）
├─ 评分（井下）
│   ├─ 评分卡录入 │ 汇总 │ 三闸面板
├─ 薪资
│   └─ 薪资总表 │ 日工资 │ 核对 │ 导出
└─ 系统
    ├─ 用户与权限 │ 表单自定义 │ 计薪参数 │ 旧数据归档
```

- 顶部常驻：**周期上下文条**（当前月 + 顺滑上/下月切换 + 懒加载），替代现有别扭选择器。
- `员工管理` 与 `OA 审批` 分离：前者是"已存在员工档案"，后者是"发起变更的流程"，二者通过事件联动。

---

## 5. 业务域与能力详述

### 5.1 员工档案（聚合根）
员工是系统的中心实体。档案页包含：
- **基本信息**（可录入/编辑，最小集起步，后续可用表单引擎扩展）：姓名、部门、岗位、技能等级、入职日、**NIDA 证件号**、电话、**NSSF 社保号**、**银行名称、银行账号、户名**（建议含，便于未来对接发薪/对账；另可扩展紧急联系人、血型等）。
- **生命周期时间线**：入职/调岗/离职/薪资变/技能变事件，按时间排序，可筛选查询。
- **请假记录**：见 5.3。
- **关联数据入口**：跳转当月薪资、出勤、产量、评分。

入职 OA 表单字段 ≈ 员工基本信息；审批通过后自动生成员工档案，之后可在档案页继续编辑。

### 5.2 OA 审批流（事件驱动）
| 流程 | 提交人 | 审批人 | 效果 |
|------|--------|--------|------|
| 入职 | 管理/班组长 | 你(KEJU) | 生成员工档案（基本信息落库） |
| 调岗 | 管理 | 你 | 改部门/轨道/岗位，带生效日 |
| 离职 | 管理 | 你 | 置离职+结束日，停止计薪，入离职表 |
| 薪资/技能变更 | 管理 | 你 | 改月薪/日薪/技能等级，带生效日 |

所有流程产出**带 `effective_date` 的事件**，系统按月推导员工当月状态，自动驱动计薪，无需二次录入。`overrides` 逐步被事件流取代。

### 5.3 请假管理

三类请假，规则互不相同：

| 类型 | 额度规则 | 审批 | 余额 |
|------|----------|------|------|
| **事假**（casual） | 按实际，影响计薪 | **需审批**（班组长代提 → 你 批准/驳回/转交） | 不累计 |
| **年假**（annual） | 满 6 个月可休 **14 天**；满 1 年可休 **28 天**带薪年假 | **无需审批** | 显示年假余额（额度 − 已休） |
| **调休**（compensatory） | 每人**每月 4 天**带薪调休，自动累积；**不是年假、不计入年假** | **无需审批** | 显示调休余额（累积 − 已休） |

- **审批流（仅事假）**：提交 → 待审 → **批准 / 驳回（需填原因）/ 转交（改派他人审）**。年假、调休为免审记录，直接落档。
- **余额管理**：年假、调休各自独立累计与展示。年假额度按司龄分档（<6 月不可休；≥6 月 14 天；≥1 年 28 天）；调休余额 = 在职月数 × 4 − 已休。
- **查询**：支持**按员工查** + **全局按时间/类型搜**（如"查所有 8 月年假"）。
- **出勤体现**：请假须在出勤表中**单独区分体现**（事假 / 年假 / 调休 三态区分，区别于 A 旷工），不混入普通出勤状态。

### 5.4 考勤
- 月度网格保留，支持手动 P/A/L 纠错（当前机制保留，无问题）。
- 请假（5.3）审批/记录后自动落对应出勤状态，并在网格中以类型区分呈现。

### 5.5 产量采集
- Web 表单直写 DB（复用现有 parser 写入的同一批表）：井下/钻工/破碎分轨道日度录入。
- 班组长仅可录**本组**数据，手机优先。

### 5.6 评分模型（井下，可选模块）
- 6 维匿名互评卡 Web 录入（无评分人列，保匿名）→ 去极值 + 管理 1.5 票 → 行为分 → 系数。
- 客观层 R1/R2 → S → 发放比例；三层奖金模型。
- 月收入 = 技能阶梯固定薪资 + 驾驶员津贴(出勤勾选+司机名单校验) + 个人奖金，并入净额。
- 防串通三闸面板前端展示。

### 5.7 薪资计算与展示
- 5 轨（井下计件/钻工/破碎/日薪/月薪）+ 评分模型统一；双路径核对保留。
- 净额公式并入出渣工人的固定薪 + 司机津贴(出勤勾选+司机名单校验) + 评分奖金。
- 薪资总表 / 日工资 / 核对 / 导出（7 Sheet）纳入出渣工人行。

### 5.8 系统管理
- 用户与权限（见第 6 节）。
- 表单自定义（见第 7 节）。
- 计薪参数（单价/NSSF 费率/奖金池等，沿用 `settings` + `/config`）。
- 旧数据归档（只读，见第 10 节）。

### 5.9 司机津贴与司机名单
- **司机名单（roster）**：独立维护的司机白名单；仅名单内人员允许驾驶、允许在出勤中勾选"驾驶"。目的：管控车辆不被乱用。
- **触发方式**：提交井下出渣工人出勤时，顺带勾选"当日是否驾驶"。设计要点：在出勤录入行增加"驾驶"勾选项；仅当该工人在司机名单内时勾选被允许，否则拦截并提示"不在司机名单"。
- **自动计薪**：勾选"驾驶"的班次自动按 5,000/班 计入该工人司机津贴，流入净额，无需手工另录。
- 司机津贴**仅产生于井下出渣工人**，其他部门不涉及；与旧通用 `_apply_driver_allowance`（按部门名/岗位含"司机"）解耦。

---

## 6. 角色与权限模型

- **预设角色**：`super_admin`(KEJU) > `admin`(中方管理) > `manager`(本地主管) > `team_lead`(班组长) > `viewer`(只读)。
- **细粒度**：权限 = **(模块, 动作, 数据范围)** 三元组；用户除继承角色外，可**单独增删**某项权限（满足"不同用户不同权限"）。
- **数据范围**：全员 / 指定班组（班组长仅本组）。
- 典型映射：班组长 = 产量+出勤+评分录入（本组）、请假代提；管理 = 全矿录入+查看；你 = 全部+审批+配置。

---

## 7. 表单引擎（配置驱动，无拖拽）

- **不做**可视化拖拽布局生成器（工作量/维护成本极高）。
- **采用 schema 驱动**：每张表单在 DB 以"字段定义"描述（字段名/类型/标签/选项/校验/必填/可见角色）。前端统一渲染器按 schema 出表单；管理端提供"选类型+填标签+设选项/必填/显隐"的简单界面维护字段。
- 标准字段落表列，自定义字段存 JSON 附列。
- 入职表单字段即员工基本信息字段，亦走此机制；日后加"紧急联系人"等，后台点几下即可，不必改代码。

---

## 8. 概念数据模型（待实现阶段细化）

| 表 | 用途 |
|----|------|
| `employees` | 员工主档（姓名/部门/岗位/技能等级/入职日/NIDA/NSSF/电话/银行名称/银行账号/户名） |
| `employee_events` | OA 生命周期事件（类型/effective_date/快照/操作人） |
| `leave_requests` | 请假（类型/提单人/审批人/状态/驳回原因/转交对象/余额联动） |
| `leave_balances` | 年假/调休余额（按规则自动计算，独立累计） |
| `attendance` | 出勤（含请假类型区分 + "驾驶"勾选，勾选须校验司机名单） |
| `driver_roster` | 司机白名单（员工ID/有效期，驾驶资格管控） |
| `production` | 产量（分轨道日度） |
| `score_cards` | 评分卡（6 维，无评分人列） |
| `form_schemas` / `form_fields` | 表单自定义 |
| `permissions` / `user_grants` | 细粒度权限 |
| `archived_kilwa` | 旧库只读镜像 |

> 现有 11 张表（`overrides`/`attendance_overrides`/`bonus_penalties`/`dismissed_employees` 等）沿用并演进；`attendance_overrides` 继续承接手动/请假覆盖。

---

## 9. 非功能需求

- **移动优先**：班组长录入页手机优先、大按钮、中英切换。
- **i18n**：中/英双语（无斯语）。
- **UX**：月份切换顺滑（上下文条 + 懒加载）；页面引入父子层级与过渡。
- **输出**：工资条、评分公示卡支持打印/导出（现金无账号）。
- **演进式**：在现有 Flask + SQLite + SPA 底座上扩展，不推倒重写。

---

## 10. 迁移策略

- 新系统启用后，**旧 `kilwa.db` 冻结为只读归档**（`archived_kilwa`），不事件化、不回溯。
- Excel 上传保留为批量导入通道（过渡期纸质→Excel→导入仍可用）。
- 新数据全从网页录入。

---

## 11. 实施路线（分阶段，每阶段可独立上线）

- **P0** 需求定稿（本说明书评审通过 + 验收标准/用户流，见 §13/§14）+ 数据模型与接口契约 + **前端框架/导航层级重建**（新 IA 与月份切换顺滑作为地基，所有后续页面均建于其上）
- **P1** 员工档案 + OA 生命周期事件化（在**新导航框架**下做通首条纵向切片：入职→审批→落档案→自动驱动计薪）→ 直击最大痛点
- **P2** 考勤网格重构 + 请假（事假审批含驳回/转交 + 年假/调休余额）+ 产量 Web 录入（班组长本组）+ 月份切换顺滑打磨
- **P3** 评分模型并入（入库 + Web 录入 + 并入净额 + 三闸面板）
- **P4** 角色细化 + 表单自定义后台 + 手机端打磨 + 全局查询
- **P5** 旧数据归档接入 + Excel 导入通道收尾 + 本地自测

---

## 12. 已决事项（需求评审结论）

1. **基本信息**：最小集起步，已含银行名称/账号/户名等（见 5.1），后续表单引擎扩展。
2. **事假审批**：需审批，支持 **批准 / 驳回（填原因）/ 转交（改派）**。
3. **请假额度规则（已明确）**：
   - 年假：满 6 个月可休 14 天；满 1 年可休 28 天带薪年假；无需审批；显余额。
   - 调休：每月 4 天带薪调休，自动累积，**非年假**；无需审批；显余额。
4. **评分模型**：延后至 **P3**（先解生命周期 + 采集痛点）。
5. **导航重构**：前移至 **P0 地基**（与数据模型同期），所有页面建于新 IA 之上。

---

## 13. 验收标准（Acceptance Criteria）

### 13.1 功能验收
| 域 | 验收标准 |
|---|---|
| 员工档案 | 可增删改查；点进档案见基本信息（含银行字段）；时间线按时间展示全部生命周期事件；改动即时保存 |
| OA 审批 | 入职/调岗/离职/薪资变/技能变可提交→审批（带生效日）；审批后员工当月状态自动变化并体现于薪资 |
| 请假 | 三类可提交；事假走批准/驳回/转交（驳回需原因）；年假/调休免审并自动累计余额；按员工+全局(时间/类型)可查；出勤表三态区分 |
| 考勤 | 月度网格可手动 P/A/L；请假自动落对应状态并区分；月份切换顺滑 |
| 产量 | 班组长仅见本组；分轨道日度录入直写 DB；与现有 parser 同源 |
| 司机津贴 | 司机名单可维护；出勤勾选"驾驶"仅名单内允许；勾选班次自动按 5,000/班 计津贴并流入薪资；非出渣工人不产生 |
| 评分(P3) | 6 维匿名卡→汇总→系数；并入净额；三闸标红 |
| 权限 | 角色预设 + 按用户增删 (模块,动作,范围)；班组长不可见他组 |
| 表单自定义 | 管理可加/改字段即时生效，无需改代码 |
| 迁移/种子 | 旧 kilwa.db 只读可查；现有在职员工可初始化进新模型；新数据全网页录入 |

### 13.2 边界与异常验收
- 防自批：审批人 = 提交人时系统拒绝。
- 额度拦截：年假/调休余额不足时提交被拦或强提醒。
- 生命周期合法性：调岗/薪资变生效日不得早于入职日；离职后不可再提交产量/请假/计薪。
- 重复保护：同员工同月不可重复入职；离职员工不可被重新发起入职（须先复职流程）。
- 权限时效：权限增删即时生效，旧 session 下次请求即受限（无越权窗口）。
- 数据隔离：班组长越权访问他组返回空/拒绝，不泄露。
- 计薪回归：事件驱动计薪结果须与旧手动 override 期一致（重构不引入偏差）。
- 司机名单校验：非名单内人员勾选"驾驶"被拦截并提示。
- 审计：所有写操作留审计日志（谁/何时/改前改后）。

### 13.3 非功能验收
- i18n：中英切换下全部新增页面文案完整无遗漏。
- 移动端：班组长录入页在手机视口可用、按钮可点。
- 性能：月份切换/全局查询体感顺滑，无卡顿。
- 演进：在现有 Flask+SQLite 底座扩展，不推倒重写。

---

## 14. 关键用户流程（User Flows）

> 标注格式：**[阶段] · 主要角色 · 前置 → 后置**

**F0 系统初始化（种子导入）** [P0/P5] · 管理 · 新系统就绪 → 在职员工进入计薪、历史数据只读
现有在职员工批量导入新模型（作为"已入职"档案 + 入职事件/基本信息）→ 旧 `kilwa.db` 冻结为只读归档。

**F1 员工入职（OA）** [P1] · 班组长/管理 → 你(审批) · 填表 → 次月计薪
填入职表单（基本信息，含银行字段）→ 提交 → 你批准/驳回/转交 → 批准后生成员工档案 + 写入"入职"事件。

**F2 调岗/薪资变** [P1] · 管理 → 你 · 在职 → 生效月自动切换
发起调岗/薪资变（填生效日，不得早于入职日）→ 你审批 → 事件落档 → 生效月自动切换部门/轨道/薪资，无需二次录入。

**F3 事假（需审批，含异常）** [P2] · 班组长 → 你 · 代提 → 出勤落"事假"
代提事假（日期/时长）→ 待审 → 批准（落出勤）/ 驳回（填原因，提交人可见并收通知）/ 转交（改派他人，新审批人收待办）。

**F4 年假/调休（免审）** [P2] · 员工/班组长 · 记录 → 余额更新
记录年假或调休 → 系统按规则校验额度（年假看司龄分档、调休看月数×4；不足则拦截/提醒）→ 余额扣减 → 出勤表显示"年假/调休" → 档案余额实时更新。

**F5 产量录入（班组长）** [P2] · 班组长 · 登录(仅本组) → 直写 DB
选月份/日期/轨道 → 填报产量 → 薪资自动取数。

**F6 司机津贴计发** [P2] · 班组长 · 工人在司机名单 → 津贴流入薪资
提交出渣工人出勤时勾选"驾驶"（须在校验司机名单内，否则拦截）→ 勾选班次自动按 5,000/班 计津贴 → 流入该工人净额，无需另录。

**F7 评分（P3）** [P3] · 管理/班组长 · 录卡 → 奖金并入净额
录匿名 6 维卡 → 去极值+管理 1.5 票 → 行为分→系数 → 奖金并入净额；三闸异常标红。

**F8 离职** [P1] · 管理 → 你 · 在职 → 停止计薪、移出在岗
发起离职 → 你审批 → 置离职日 + 停止计薪 + 入离职表 + 从在岗列表移除。

**F9 月度关账/导出** [P3/P5] · 管理 · 当月数据齐 → 可发薪/归档
核算 → 双路径核对（0 偏差）→ 导出工资条/评分公示卡 →（可选）现金发放签收记录。

**F10 表单自定义** [P4] · 你(管理) · 表单引擎就绪 → 字段扩展无需改码
管理加字段（类型/标签/选项/必填/显隐）→ 新员工表单即时出现新字段 → 历史记录该字段为空（兼容）。

**F11 权限异常流** [P4] · 班组长(越权) · 已配置范围 → 越权被拦留审计
班组长尝试访问他组员工/数据 → 系统拒绝或返回空，不泄露。

**F12 权限调整** [P4] · 你 · 配置 → 即时生效（旧 session 受限）
系统→用户与权限→选用户→增删 (模块,动作,范围)。

## 15. 成功指标（KPI，可选）
- 调岗/薪资变：从跨系统多次手动操作 → **1 次网页操作即生效**。
- 采集：出勤/产量/评分从 Excel 上传 + 纸质 → **网页直录**，班组长手机可完成。
- 争议：评分/工资条可打印公示，工人异议有迹可查（审计 + 时间线）。
- 车辆：仅司机名单内人员可计驾驶津贴，乱用可追溯。

