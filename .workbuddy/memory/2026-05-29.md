# 2026-05-29 工作日志

## Kilwa Nickel 管理系统完善

### 完成内容
- 创建 `core/addressbook.py`：通讯录解析 + 基础信息表解析
- 重写 `app.py`：通讯录集成、预支补全、导出Excel、通讯录API、端口检测、auto_load_source 自动加载、scan_source_files按内容特征识别文件、/reload重新加载、/set-month月份切换
- 重写 `templates/index.html`：通讯录页面、导出按钮、员工搜索筛选、重复槽位警告、页面自动检查已加载数据
- 修复 `core/calculator.py`：浮点数取整
- 创建 `start.sh`+`~/.zshrc` alias `kilwa`/`kilwa-stop`：一键启动/后台常驻/关闭
- 源文件固定位置：`data/source/`，启动自动加载解析和计算，文件名无关识别
- **大改UI**：暗色科技风（OPEN CODE风格），毛玻璃卡片、霓虹渐变、数据台首页、月份筛选、图标标签切换、Toast通知
- 清理 `uploads/` 临时目录，移除上传页面改为数据台
- 新增：`/employees/save-rate` 保存日薪/月薪基数、`HARD_EXCLUDE_IDS`硬排除5人、`strip_dept`去掉ENPRIZON前缀
- 新增：员工备注标记（考勤有/通讯录无→"已离职"；通讯录有/考勤无→"本月无考勤"）
- 前端：单元格编辑自动保存、备注列
- 删除通讯录页面、新增部门筛选到员工管理页、新增 `/export/employees` 员工信息导出
- 新增 NSSF（社保）模块：`core/nssf.py`、`/nssf/list`、`/nssf/toggle` API、计算扣除10%、导出含NSSF列
- 更新备注栏仅显示系统备注（已离职/无考勤）、新增NSSF开关按钮
- 新增计算控制面板：`core/pricing.py`、`/config` API、前端配置页（修改单价/NSSF费率自动重算）
- 新增出勤页面：`/attendance` API（从Piece Rate表构建出勤网格）、前端出勤展示页、手动勾选出勤/缺勤
- 修复：例外保存后员工页"当前类型"不同步、薪资表不刷新
- 修复：出勤网格只显示1个人的缩进bug

### 验证结果
- 上传+解析+计算全流程通过
- 130名员工、14,974,000 TZS 应发合计、3,305,500 TZS 预支扣除
- 导出Excel正常（13.8KB，含3个子表）
- 通讯录125人，7个部门分组
- **SQLite 迁移完成**：所有数据（例外、NSSF、出勤、配置）存储在 `data/kilwa.db`
- **重启不丢失数据验证通过**：override_type、出勤标记、配置参数全部持久化
- **每月数据历史**：新增 `monthly_data` 表，每次加载自动存入工资结果，切月份优先从 DB 读
- 员工页合并"默认类型/当前类型"为一列，薪资页新增姓名/类型筛选
- 修复：月薪员工标记 A/L 后按比例扣减（每缺勤1天扣1/30）
- 修复：重新计算不再跳转到薪资页
- 计件工人也可手动标记 A/L，标记后不计入当天产量分配
- 员工筛选改为按当前类型（override_type）筛选
- UI 科技感全面升级：深空黑底色、毛玻璃卡片、渐变图表、涟漪动效、霓虹配色
- 导航栏 SVG 线性图标替代 emoji，高级图表配色（柔蓝/薄荷绿/玫瑰粉/薰衣草紫/琥珀黄）
- 数据台布局重组：统计数据 → 全宽产量趋势图 → 钻工排名+耗材 → 源文件/操作 → 快捷入口
- 移除独立的"产量"页面，图表集成到数据台
- 出勤页恢复 D(白班)/N(夜班)/B(全天) 原始标识，手动点击进入 P→A→L 循环
- 例外标记支持全部 4 种薪资类型（日薪/月薪/井下计件/钻工计件），计件类型无基数设置

### 启动方式
```bash
cd /Users/osong/WorkBuddy/kilwa-system && python3 app.py
```
