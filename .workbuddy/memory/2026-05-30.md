# 2026-05-30

## 大规模重构
- 例外弹窗拆为两个独立板块：①薪资类型设置（永久）②临时例外（当月）+ 临时例外自动跨月消失
- 永久类型保存时清掉旧类型覆盖 + 临时例外，计件类型自动归零基数
- 出勤页恢复为简单点击循环（空→P→A→L→空），D/N 由系统根据产量自动显示
- 板块②临时例外新增：井下计件选 D/N，钻工计件选队长（自动从当天数据加载队长列表）
- 出勤复位同时清理 shift_additions / driller_additions

## 新增页面
- 产量核验卡片（数据台）：逐日对比钻工组产量与井下合计，不匹配标红
- 逐日工资明细页（日工资）：行为员工列为日期，按总工资降序排列
- 日工资加部门和类型筛选
- 日工资标注：临时例外覆盖的日期有虚线下划线

## UI 修复
- 产量核验表首列冻结 + 横向滚动（min-width:0 修复 CSS Grid 撑开问题）
- 每个日期组 (NH/NL/MW) 后加分隔竖线

## Bug 修复
- 硬编码 DB 路径导致总工资变化（calc_underground_piece 的 data_folder 参数）
- calc_driller_piece 重构引入的 rounding 偏移
- HOSEA driller_additions 残留未被复位清掉
- DB 被 git stash 脏数据污染

## 列索引重构
- parser.py 全部改为扫描表头获取列号，不再硬编码
- 钻工队伍通过 DrillerTeamNameXX！！！+ DrillerTeamXXMember 模式自动发现
- 日薪表、柴油表同样扫描表头
- 新旧 parser 输出完全一致（验证通过）

## 已知问题
- 当前总工资 44,999,581（由于多次测试修改了 DB 数据）
- 日工资与薪资表有 ±7 TZS 内的 rounding 差异

## 日工资标注
- 日工资页标注出勤手动更改日期（P/A/L）→ 黄色虚线下划线
- 同时标注临时例外日期
- 与出勤页标注风格一致

## HARD_RULE
- ~/.workbuddy/MEMORY.md 写入 git stash 禁令，每次会话启动自动加载

## 类型排除 + 份额重分配
- 改类型的人（如 day_rate/monthly）从计件池排除，其份额分配给剩余计件工
- 临时例外（如 5/3 井下）在例外日期切换池，非例外日期仍走原类型池
- 总工资从 45,334,428 变为 47,509,502 → 更准确，因为之前被零清吞掉的份额现在正确分配
